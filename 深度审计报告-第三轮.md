# 深度审计报告 — 第三轮（服务端 + 安全）

- 审计对象：`D:/Desktop/rongyi/source/apps/v2`（Electron 43 + React + Express 5 + better-sqlite3）
- 审计基线：第二轮报告 §2/§3/§6/§11 + `git log`（最近修复提交：`de1fcca`、`c9d35a9`、`e6f80ff`、`62c1edb`、`68799bd`、`23ba772` 等）
- 方法：静态代码逐行核对 + 提交 diff 比对 + 路由/资源/安全配置交叉验证；本报告仅收录亲眼验证的结论
- 结论速览：**无新增高危**；中危 4 项、低危 4 项、提示 6 项；第二轮高危/中危 9 项中 7 项已修、2 项部分修

---

## 一、修复验证结果（第二轮）

### 1.1 已修（7 项核心修复全部确认落地）

**H1（已修）** 财务资源 roles 对齐 + 写保护字段补齐
- 证据：`src/server/infrastructure/security.ts:36-74` `PROTECTED_WRITE_FIELDS` 已含 `discountRate / maxDiscountAmount / annualDiscountLimit / totalFee / settleStatus / settledAmount / settledAt`；`src/domain/resources.ts` 中 `memberCards / processingOrders / charges / chargeItems / debtRecords / refunds / memberCardLogs / memberPointLogs / purchaseOrders` 等财务资源均收为 `roles: boss`；`src/server/http/router.ts:27` 通用 CRUD 入口校验 `resource.roles`，DOCTOR 访问返回 403；`router.ts:191` `csvCell` 带公式注入防护；`router.ts:139-150` 保留 M2 患者/病历删除保护。
- 影响：DOCTOR 无法再经通用 CRUD 直写财务资源。

**H2（已修）** 治疗方案 bill/discount 改收 financeStaff
- 证据：`src/server/http/route-policy.ts:39` 新增 `^/api/v2/treatment-plans/[^/]+(\/items\/[^/]+)?\/(bill|discount)(\/|$)` → `roles: ['BOSS']`；`src/server/http/app.ts:355-362` 用 `find` 取**首条**匹配策略，该规则在 :42 临床规则之前，优先命中。
- 残留（外观）：`treatment-plan-billing-routes.ts:8` 注释仍写 `（clinical）`，属过期注释，不影响行为。

**M4（已修）** 最后 active BOSS 保护
- 证据：`src/server/application/service-modules/auth.ts:323-328` `updateUser` 在禁用/降级 BOSS 前按 clinic 统计 active BOSS，≤1 时抛 `ValidationError('不能禁用或降级最后一个管理员(BOSS)账号')`；`resources.ts:73` `users` 资源 capabilities 全 false，堵死通用 CRUD 绕过。

**M5 会话族吊销（已修）**
- 证据：`auth.ts:85-96` refresh 重用检测（事务内双重检查）→ `revokeReplayedFamily`；`src/server/infrastructure/repositories/core.repositories.ts:229-235` 清 refreshToken + tokenVersion+1（全部 access token 失效）；`auth-admin.ts:54-63` refresh 失败有审计。

**配额按诊所（已修）**：`src/server/application/service-modules/files.ts:29-37` 配额查询加 `tenantAnd(clinicId)`。

**sync 复合游标（已修）**：`sync.ts:45-58` `(createdAt > ? OR (createdAt = ? AND rowid > ?)) ORDER BY createdAt ASC, rowid ASC LIMIT 1000`，游标格式 `createdAt|rowid`，旧格式兼容；push 每 500 条一批 `BEGIN/COMMIT/ROLLBACK`，带 `code` 的 SQLiteError 回滚整批。

**legacy 导入（已修）**：`legacy-import.ts:50/:62` 只删 target 侧车；`sqlite-files.ts:14-38` `backupSqliteFile` 先 `wal_checkpoint(TRUNCATE)` 再 `VACUUM INTO`，失败 `logger.warn` 告警并回退 `copyFileSync`，不删除源库 WAL。

**wechat TLS（已修）**：`wechat.ts:58-60` send 时校验 https；`:87-90` 环境变量非 https 直接禁用通道并 `console.warn`；5s 超时 `AbortController`。

**CSV 公式注入 + 电话掩码（已修）**：`operations.ts:350-356` `csvCell` 防护 `^[=+\-@\t\r]`；`:359-364` `maskPhoneForExport`（前 3 后 4 掩码，`remindersCsv:232` 已应用）；前端 `src/web/AnalyticsDashboardPage.tsx:50-57` 同样修复。

**scheduler（已修）**：全部任务 try/catch + `logger.error`；自动备份失败创建 CRITICAL 业务告警（onAlertCreate）；`isRunning` 防重入；`stop()` 清全部定时器。

**审计覆盖（已修）**：`auth-admin.ts` login 成功/失败、refresh 成功/失败、logout 均审计；`app.ts:333-354` 非 GET 请求全量审计中间件（`maskSensitiveFields` 脱敏 body）；审计 flush 失败重试一次（`app.ts:152-172`）。

**express 5 路由层（基本正常）**：`middleware.ts:108-112` `wrapAsync` + Express 5 原生 async 错误传播；`app.ts:295` json 2mb 限制；`errors.ts:46-51` 413/400 正确映射；`app.ts:419-421` 404 处理器存在。

**第二轮隐藏坑#11（部分修）**：见 1.2。

### 1.2 部分修（3 项）

**小数单价（部分修）**：`financial.ts` ChargeService.create:91、PurchaseOrderService.create:517、ProcessingOrderService.create:681 均以 `!Number.isSafeInteger(unitPrice)` 拒绝小数 ✓；**但** `financial.ts:669` ProcessingOrder `totalFee` 仍只 `Number.isFinite`，`:709` `Math.round(Number(input.totalFee))` 静默取整（100.5 → 存 100）。API 直调 totalFee=100.5 时明细与合计不一致。→ 见新发现中危 #4。

**CSP（部分修）**：`electron/main.cjs:750` 响应头注入精确 `http://127.0.0.1:<apiPort>` ✓；但 `index.html` meta 仍保留 `connect-src http://127.0.0.1:*`，注释声称依赖响应头交集收紧。**file:// 加载时 `onHeadersReceived` 是否对 file: 协议注入 CSP 未实证**（依赖 Electron 未验证行为，见提示 #1）。

**expiring days 上界（部分修）**：`src/server/http/routes/workflow.ts:284-289` `/api/v2/inventory/expiring` 已钳制 `Math.min(Math.max(Math.floor(days),1),3650)` ✓；但 `src/server/http/routes/inventory-batch-routes.ts:21-24/:94-97` 只做 `Number.isFinite` 检查，`inventory-batch.ts:338-345` `normalizeDays` 也仅下限 ≥0 无上界 → 超大 days（如 10^9）可对批次表做大范围扫描。→ 提示 #5。

---

## 二、新发现问题

### 中危（4 项）

**中危 #1 — treatmentPlanItems 的 price/billed/billedChargeId 可经通用 CRUD 被 DOCTOR 改写**
- 位置：`src/domain/resources.ts:328-343`（treatmentPlanItems 定义）+ `src/server/infrastructure/security.ts:36-74`（PROTECTED_WRITE_FIELDS 缺失项）
- 证据：treatmentPlanItems `roles: clinical`（DOCTOR 可写）、`capabilities: { update: true }`；`PROTECTED_WRITE_FIELDS` 为全局 Set，仅覆盖 `totalFee/discountRate/...` 等，**不含 `price/billed/billedChargeId/amount/unitPrice`**。而 `treatment-plan-billing.ts:206-208` bill 时按明细**实时价格**计算实收金额；`billed` 置 true 可伪造"已划价"。
- 影响：DOCTOR 经 `PATCH /api/v2/treatmentPlanItems/:id` 改 `price` 直接影响 bill 实收金额（财务完整性）；置 `billed=true` 干扰正常收费流程。同步放大：`chargeComboItems`（resources.ts:837-）同理可存/改小数 price，经 `charge-combo.ts:61` `Number(item.price)` 透传——`ChargeService.create:91` 会拦截小数（400），但**不产生静默错误，仅延迟暴露**，源头未校验。

**中危 #2 — sync push 放行 Charge DELETE，绕过 cancel 业务规则**
- 位置：`src/server/application/service-modules/sync.ts:109-113`
- 证据：`if (change.tableName === 'Charge' && change.operation !== 'DELETE')` 拦截，即**仅拦截非 DELETE**；`DELETE` 操作被放行进入通用软删路径。
- 影响：BOSS 设备可经 sync 软删**任意状态** Charge（含 PAID/REFUNDED），绕过 `charge.cancel` 的"仅 UNPAID 可取消"规则，且无对应审计与资金联动，破坏对账一致性。

**中危 #3 — refund 驳回/取消冲销扣错会员卡**
- 位置：`src/server/application/service-modules/refund-flow.ts:144-150`（冲销）vs `src/server/application/service-modules/financial.ts:293`（回充）
- 证据：回充路径精确取原卡（`charge.memberCardId` → `findByPatientForRefund`）；而 `reversal` 冲销时取患者**第一张 ACTIVE 卡**（`ORDER BY createdAt LIMIT 1`，refund-flow.ts:146-149）直接 `balance - amount`。
- 影响：多卡患者退款被驳回/取消时，从错误卡片扣回余额（资金错位）；若第一张 ACTIVE 卡余额不足则 `Math.max(0,...)` 静默截断，错位金额永久丢失。附带：reversal 的 Charge 查询（refund-flow.ts:128-130）无 `tenantAnd`（风险低，refund 已按诊所过滤，但纵深防御缺失）。

**中危 #4 — ProcessingOrder.totalFee 小数静默取整**
- 位置：`src/server/application/service-modules/financial.ts:669/:709`
- 证据：`:669` 仅 `Number.isFinite(totalFee)`；`:709` `Math.round(Number(input.totalFee))` 把 100.5 静默存为 100，而明细 `unitPrice` 已在 :681 拒绝小数——同一单据两套口径。
- 影响：API 直调可产生"明细合计 ≠ 订单 totalFee"的单据，财务统计（技耗分离/对账）出现无法解释的差额；前端正常路径不受影响。

### 低危（4 项）

**低危 #1 — L1 电话直出残留**
- 位置：`src/server/application/service-modules/wechat-reminder.ts:248/:269`（直接输出 `patientPhone` 至提醒内容）；`src/server/http/read-routes.ts:134-148`（`appointments/by-date` 用 `SELECT t.*` 直出 `tempPatientPhone`）
- 证据：与第二轮 L1 相同模式，未声称修复，确认残留。
- 影响：患者手机号明文出现在提醒文案/接口响应中，未按掩码策略统一。

**低危 #2 — member-discount 年度限额按 UTC 年归属**
- 位置：`src/server/application/service-modules/member-discount.ts:220-225`（`strftime('%Y', createdAt)` 按 UTC 年份分组）
- 证据：`clinicDate()` 时区（东八区）下 1 月 1 日 00:00–08:00 的消费被计入**上一年**限额；第二轮隐藏坑 #1 未修。
- 影响：跨年首日限额计算偏差；配套问题：`quote()` 经 `findCard` 不校验 `status === 'ACTIVE'`（仅 quoteByPatient 校验）；年度限额为纯咨询性质，charge 创建不强制。

**低危 #3 — refund/charge-combo 金额路径源头缺整数校验**
- 位置：`src/server/application/service-modules/charge-combo.ts:61-62`（`Number(item.price)/Number(item.quantity)` 无校验）、`refund-flow.ts` approve/reject 金额均来自库内 `Refund.amount`（无创建侧校验复核）
- 证据：chargeComboItems 经通用 CRUD 创建（resources.ts:837），`price` 字段无 min/safe-integer 约束；组合应用时依赖 `ChargeService.create:91` 拦截。
- 影响：错误延迟到应用时才报 400，组合本身可保存脏数据；建议在组合保存侧补 `isSafeInteger`。

**低危 #4 — 服务端无幂等键，发药/退款"防重"仅前端**
- 位置：`src/web/use-async-action.ts`（前端 in-flight 防重，提交 `68799bd` 引入）+ 服务端 `dispense.ts`/`refund-flow.ts` 无幂等 token
- 证据：`68799bd` 对 dispense/refund 的改动实为**分页**（list/count）+ 前端防重钩子；服务端无 requestId/唯一约束幂等。
- 影响：网络重试/并发双击可重复创建发药单或退款申请（库存扣减有 `ConflictError` 兜底，不会超发，但会产生重复单据）；属纵深防御缺口。

### 提示（6 项）

**提示 #1 — CSP meta `connect-src 127.0.0.1:*` 依赖 file:// 头注入（未实证）**
- 位置：`index.html` meta 标签；`electron/main.cjs:750`
- 影响：若 `onHeadersReceived` 对 file: 请求不生效，CSP 实为 `:*` 通配；需在 Electron 内实测一次（Session.webRequest 拦截 file:// 响应）后关闭本项。

**提示 #2 — CORS 仍允许 `origin === 'null'` 与 file://**
- 位置：`src/server/http/app.ts:274-277`
- 影响：任意本地文件（file:// 页面）可发跨源请求到本地 API；Electron 生产环境默认禁 webSecurity 的情况才构成实际风险，第二轮 L4 未声称修复，保留提示。

**提示 #3 — 分页参数 NaN 导致 500**
- 位置：`src/server/application/service-modules/refund-flow.ts:27-28`、`dispense.ts:200-201`
- 证据：`Math.max(1, Number('abc')) = NaN` → better-sqlite3 绑定 NaN 抛 TypeError → 500（应为 400）。
- 影响：畸形 query 触发 500，日志噪音；建议统一 `sanitizePage`。

**提示 #4 — 方法不匹配返回 404 而非 405**
- 位置：`src/server/http/app.ts:419-421`
- 影响：无 405 区分，API 客户端排障体验差；无安全后果。

**提示 #5 — inventory-batch days 无上界**
- 位置：`src/server/http/routes/inventory-batch-routes.ts:21-24/:94-97` + `inventory-batch.ts:338-345`
- 证据：路由仅 `Number.isFinite`，服务层 `normalizeDays` 仅下限 ≥0；与已修的 `workflow.ts:284-289`（1..3650 钳制）不一致。
- 影响：超大 days 触发全表范围扫描（性能）；无数据破坏。

**提示 #6 — treatment-plan-billing-routes.ts:8 过期注释**
- 位置：`src/server/http/routes/treatment-plan-billing-routes.ts:8`
- 影响：注释仍标 `（clinical）`，与 route-policy（financeStaff）不符，误导后续维护。

---

## 三、5 条最优先项

1. **【中危 #1】封堵 treatmentPlanItems 通用 CRUD 金额绕过**：将 `price/billed/billedChargeId`（及全局 `amount/unitPrice/quantity` 类）纳入 `PROTECTED_WRITE_FIELDS`，或对 treatmentPlanItems 关闭 update、改为专用路由；同时给 `chargeComboItems.price` 补 `isSafeInteger` 校验。一处修复可同时消除 bill 金额篡改与组合脏数据。
2. **【中危 #2】sync push 禁止 Charge DELETE**：`sync.ts:109` 改为 `change.tableName === 'Charge'` 全量拦截（DELETE 也拒），软删请走 `charge.cancel` 专用路由，保证状态机与审计完整。
3. **【中危 #3】reversal 按原卡冲销**：`refund-flow.ts:144-150` 复用回充取卡逻辑（`charge.memberCardId` 优先，缺失时按患者+卡号匹配，而非 `ORDER BY createdAt LIMIT 1`），并在余额不足时抛错而非 `Math.max(0)` 截断。
4. **【中危 #4】ProcessingOrder.totalFee 统一整数口径**：`:669` 改为 `Number.isSafeInteger`，删除 `:709` 的 `Math.round` 静默取整，改为拒绝非法输入。
5. **【低危 #1 + 提示 #3】隐私与健壮性小修**：`wechat-reminder.ts:248/:269` 与 `read-routes.ts:134-148` 输出 `maskPhoneForExport` 统一掩码；分页参数统一 `sanitizePage`（修复 NaN→500）。同时实测 CSP file:// 注入效果（提示 #1），确认后收紧 `index.html` meta 中的 `:*` 通配。

---

### 附：本报告新确认事实（相对第二轮新增）
- 第二轮隐藏坑 #11（expiring days）已修一半：`workflow.ts` 路由已钳制，`inventory-batch` 路由/服务层未修（提示 #5）。
- `68799bd` 的"防重"实为前端 `useAsyncAction` + 服务端分页，服务端无幂等键（低危 #4）。
- `PROTECTED_WRITE_FIELDS` 是全局 Set 且不含 price 类字段，是本轮最值得系统性补齐的一点（中危 #1 根因）。

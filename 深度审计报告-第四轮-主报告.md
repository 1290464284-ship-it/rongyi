# 第四轮全面深度审计主报告

日期：2026-08-07 | 范围：`source/apps/v2`（Electron 43.2.0 + React 19 + Express 5 + better-sqlite3）+ 外层治理仓库
方式：修复轮完成后自动开启的验证式审计——逐项核对第三轮 P0/P1/P2 修复落地、亲测全部门禁（typecheck / lint / knip / security-scan / 全量测试）、评估两个有意未修项、检查回归
基线（修复轮结束后实测）：typecheck 服务端+前端 ✅、lint 0 errors（39 warnings 均为既有 react-refresh/exhaustive-deps）✅、knip 0 未用导出 ✅、security:scan 358 文件通过 ✅、全量测试 139 文件 / **1272/1272 通过**（基线 1265 + 本轮新增 7）✅

---

## 一、最重要结论（先说）

1. **第三轮 CI 门禁全红问题已彻底解决**：本地四道门（lint / knip / security-scan / 测试）+ typecheck 全部实测通过，且 pre-push 钩子已扩展为 typecheck→lint→knip→security-scan→test→build 六道门。第三轮报告的 P0 除"推送分支"（用户未授权，见下）外全部完成。
2. **唯一未完成事项是有意的**：`codex/v2-full-optimization` 仍从未推送（领先 origin/main 327 提交）。第三轮报告与 P0-2 均建议推送，但用户未授权 push，本代理遵守约束不推送。**这是剩余的最大流程风险，建议用户明确分支策略**（推送该分支 or 合并到 main）。磁盘损坏即 10 天工作全丢。
3. **修复轮提交干净利落**：服务端 15 文件 1 提交（fa54e14）+ 前端 25 文件 1 提交（199eb9f），pre-commit 钩子自动跑全量测试且两次均 1272/1272 通过；工作区已完全干净（source 与外层均无未提交改动）。
4. **两个有意未修项经本轮复核维持原决策**：通用列表 `tempPatientPhone` 未掩码（会破坏前端编辑回填，需专门设计）、`dispense.ts` 药剂师列表 `patientPhone` 直出（工作流依赖）。详见 §三。

---

## 二、第三轮修复逐项验证（P0/P1/P2 全覆盖）

### P0（立即）

| 项 | 结果 | 证据 |
|---|---|---|
| P0-1 ReportDialog 保存清空数据 | ✅ 已修 + 回归测试 | `ReportDialog.tsx`：`effectiveJsonText = jsonText ?? (reportQuery.data ? JSON.stringify(loadedReport, null, 2) : DEFAULT_REPORT_JSON)`；null 时提示"报告数据尚未加载完成"；`CephalometricPage.spec.tsx:164` 新增"未编辑直接保存不丢数据"回归测试（实测通过） |
| P0-2 推送分支 | ⏸️ 未执行（用户未授权 push） | 见 §一-2 |
| P0-3 smoke 凭据统一 | ✅ 已修 | seed 默认 `ry0801`，全部 4 个 smoke 脚本统一为 `V2_ADMIN_PASSWORD ?? 'ry0801'`（upgrade-smoke.ps1:89 已确认） |
| P0-4 CI 四道门 | ✅ 全绿（实测） | lint 0 errors / knip 0 / security-scan passed (358 files) / 测试 1272/1272；pre-push 钩子含全部六道 |
| P0-5 sqlite.bak 出库 | ✅ 已修 | 外层 `git rm --cached` sqlite.bak/-shm/-wal + `.qoder/better-harness/`，.gitignore 追加规则；git gc 后 8.15 MiB |

### P1（本轮内）

| 项 | 结果 | 证据 |
|---|---|---|
| P1-6 treatmentPlanItems 金额保护 | ✅ 已修 + 加强 | `security.ts:76-77` billed/billedChargeId 入 PROTECTED_WRITE_FIELDS（price 不入全局，保留前端写未划价明细）；`router.ts:129-133` 已划价明细 PATCH price→409"已划价明细不可改价"、quantity→409"已划价明细不可修改"；**本轮额外加强** `router.ts:161` DELETE 已划价明细→409"已划价明细不可删除"（金额凭证防伪造）。router.spec.ts 对应测试 13/13 通过 |
| P1-7 sync 禁 Charge DELETE | ✅ 已修 + 加强 | `sync.ts:109-113` Charge 全操作（含 DELETE）拒绝，错误 'Charge writes are disabled in sync; use charge APIs'；services-edge.spec.ts 新增 DELETE 场景断言 |
| P1-8 refund 冲销原卡 | ✅ 已修 | `refund-flow.ts` 优先取 `charge.memberCardId` 原卡，缺失回退第一张 ACTIVE 卡并 console.warn（多卡场景不再扣错卡） |
| P1-9 totalFee 整数 | ✅ 已修 | `financial.ts:669` `Number.isSafeInteger` 拒绝小数（12.5 → ValidationError），709 行 Math.round 仅对已校验整数做防御性转换；services-edge.spec 新增 12.5 拒绝测试 |
| P1-10 暗色主题对比度 | ✅ 已修 | styles.css：亮色 `--primary #0D8282`（4.63:1）、`--warning #A56310`（4.78:1）；暗色 `--primary #14A0A0`（5.17:1）、`--primary-hover #17B4B4`、`--border-strong #507078`（3.09:1）全部达标；OutlineSvg/CompareResultView 硬编码 #f8fafc/#c9d3de → var(--surface)/var(--border-strong) |

### P2（低危/长期）

| 项 | 结果 | 证据 |
|---|---|---|
| P2-11 电话掩码收尾 | ⚠️ 部分修 | wechat-reminder.ts:270 ✅、read-routes.ts by-date 路由 ✅（152 行 maskPhoneForExport，保持响应结构）；**通用列表未掩码**（有意未修，见 §三-1） |
| P2-11 分页 NaN→400 | 未处理 | 属低危（内部 API 5xx 非数据风险），未列入本轮，建议第五轮评估 |
| P2-11 days 上界 | ✅ 已修 | `inventory-batch.ts:342` `Math.min(Math.max(Math.floor(value), 1), 3650)` |
| P2-11 UTC 年归属 | ✅ 已修 | `member-discount.ts:222` 按诊所时区（+8）统计；spec 改为 delta 断言消除共享 db 干扰 |
| P2-12 @types/node | ✅ 已修 | ^26.1.2 → ^24.13.3（package.json + pnpm-lock.yaml 已入库） |
| P2-12 CSP 实测 | 未处理 | file:// webRequest 响应头注入在生产面未实证；建议第五轮在打包环境实测 |
| P2-13 死代码 | ✅ 全清 | knip 0：服务端 6 导出去 export + 11 类型去 export + 1 整行删除；前端 `validFormItems` 去 export（本轮最终清零）；`buildItemPayload/isItemUnchanged` 去 export；`CephalometricCompareCase/ChargeComboItemRow/ProcessingStepStatus` 因同文件内被引用保留（已确认） |
| P2-13 splitList 消重 | ✅ 已修 | 5 份重复（plan-utils/items.ts/MedicalRecordsPage/TreatmentsPage）统一收编 `format.ts`，全部导入已更新；joinList 保留（有使用方） |
| P2-14 .qoder/better-harness 出库 | ✅ 已修 | git rm --cached + .gitignore |
| P2-14 spec.md 过期路径 | ✅ 已修 | 提交 7997090 |
| P2-14 git gc | ✅ 已修 | 8.15 MiB |
| P2-15 LoginPage.spec 更新 | ✅ 已修 | 新增 3 个记住我测试（记住/不记住/预填），初始值来自 localStorage 的干扰已通过先取消勾选修正 |
| P2-15 Dialog 出口统一 + 竞态 | ✅ 已修 | components.tsx 渲染期复位 setClosing + closeEpochRef 代际守卫（effect 内递增，迟到关闭定时器丢弃）；CrudPage 加 dialogEpoch key 重挂载消除 closing 140ms 窗口吞"新建"（react-hooks v6 禁止渲染期读 ref，故用 useState 记 prevQueryData 模式） |
| P2-15 DashboardPage 双重导入 | ✅ 已修 | hub-tabs.tsx 改静态导入（App.tsx 已静态引入，消除重复 lazy + build INEFFECTIVE_DYNAMIC_IMPORT 警告） |
| 第三轮-额外 | ✅ 已修 | electron/error.js 删除 + error.html 引用移除；security-scan document.write → blob URL（AnalyticsDashboardPage）；前端 lint 8 errors 清零（`_parsedMetrics`/`_file`/`_inventoryRows` 等）；ChargesPage 恢复 deleteCharge 真删路径（此前按钮存在但无人调用 = 静默失效）+ ConfirmDialog 确认框；Layout 全局搜索 placeholder 改"搜索患者…"（与真实行为一致）；服务端 4 处 lint 修复 |

**回归检查**：修复轮完成后全量测试 1272/1272（基线 1265 + 服务端子代理 3 + 主代理 4），typecheck 双配置通过，lint/knip/security-scan 全绿，**无任何回归**。

---

## 三、两个有意未修项评估（复核维持原决策）

### 1. 通用列表 `tempPatientPhone` 未掩码（中低危，需专门设计）

- **现状**：by-date 路由已掩码（read-routes.ts:152）；但通用列表 `/resources/appointments` 仍直出原始 `tempPatientPhone`。
- **为什么不能直接掩码**：`AppointmentsPage.tsx:203` 用列表行数据回填编辑表单（`String(row.tempPatientPhone ?? '')`），若掩码，保存时会把 `138****0000` 掩码值写回数据库，**破坏真实数据**。
- **可选方案**（供第五轮/用户决策）：
  a) 通用列表掩码 + 前端编辑时改调 GET 详情接口取原始值（需确认详情接口是否也掩码，若掩码则需白名单角色豁免）；
  b) 掩码仅对非医生/非管理员角色生效（响应按角色分层）；
  c) 保持现状并在审计中持续跟踪（手机号属敏感个人信息，但本项目为本地单机诊所软件，风险可控）。
- **结论**：维持不修，标注为第五轮审计跟踪项，需与产品（编辑回填流程）一起设计。

### 2. `dispense.ts:204` 药剂师列表 `patientPhone` 直出（低危，产品决策）

- **现状**：Dispense 列表 LEFT JOIN Patient 直出 `P.phone AS patientPhone`。
- **为什么不掩码**：药剂师发药核对依赖患者姓名+电话（取药核对工作流）；掩码后无法核对身份。该接口已限登录用户，且门诊场景电话是工作必需信息。
- **结论**：维持产品决策不修；若未来对接外部系统或增加非工作角色，再评估。

---

## 四、本轮新发现（修复轮及验证过程中）

| 级别 | 发现 | 说明 |
|---|---|---|
| 低 | pre-push 钩子含 `build`，推送时全量 build 耗时较长 | 功能正确（门禁更严），仅体验问题；若不接受可去掉 build 保留其余五道 |
| 低 | 分页 NaN→500 仍未处理 | 第三轮 P2 遗留，无数据风险；建议第五轮补 400 防护 |
| 低 | CSP file:// 生产生效面仍未实证 | 第三轮 P2 遗留；建议打包环境实测后再决定是否收紧 |
| 信息 | `CephalometricCompareCase`/`ChargeComboItemRow`/`ProcessingStepStatus` 保持 export | 被同文件内已使用类型引用，knip 已确认非未用；去 export 会破坏类型引用，维持现状正确 |
| 信息 | member-discount.spec 改 delta 断言 | 消除了共享 db 并行干扰，测试稳定性提升 |

---

## 五、当前健康度快照

- 测试：**1272/1272 全绿**（139 文件，21.8s 运行）
- 门禁：typecheck ✅ lint 0 errors ✅ knip 0 ✅ security-scan ✅ pre-push 六道 ✅
- 工作区：source 与外层均干净，无未提交改动
- 提交：修复轮 2 个（fa54e14 服务端 15 文件 / 199eb9f 前端 25 文件）+ 基础设施轮 8 个（含 de3ae1f 外层），全部含测试验证
- 剩余风险排序：
  1. **327 提交未推送（唯一副本）**——最高风险，需用户决策
  2. 通用列表 tempPatientPhone 未掩码（需专门设计）
  3. 分页 NaN 400、CSP 实证（低危）

---

## 六、建议

1. **用户尽快决策分支策略**：推送 `codex/v2-full-optimization` 或合并 main（P0-2 唯一遗留）。推送前 pre-push 六道门会自动验证。
2. 第五轮审计重点：通用列表掩码方案设计（§三-1）、分页 NaN→400、CSP 打包实证、pre-push build 耗时评估。
3. 若进入第五轮修复：以上三项均为低危，无 P0 级新问题。

—— 第四轮审计结束，等待用户指令决定是否继续下一轮（当前建议：先决策推送策略）。

# API 错误码说明文档

## 1. 错误响应格式

### 1.1 统一响应结构

所有 API 错误响应遵循统一的 JSON 格式：

```json
{
  "success": false,
  "statusCode": 400,
  "code": "GEN_001",
  "errorCode": 1006,
  "message": "参数校验失败",
  "errors": ["name 不能为空", "phone 格式不正确"],
  "details": {
    "field": "email"
  },
  "timestamp": "2024-01-01T12:00:00.000Z",
  "path": "/api/patients",
  "traceId": "abc123-def456"
}
```

### 1.2 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `success` | boolean | 始终为 `false`，表示请求失败 |
| `statusCode` | number | HTTP 状态码 |
| `code` | string | 字符串格式的业务错误码（遗留系统兼容） |
| `errorCode` | number | 数字格式的错误码（新标准） |
| `message` | string | 错误描述信息（已脱敏） |
| `errors` | string[] | 参数校验错误详情（仅校验错误时返回） |
| `details` | object | 附加错误详情（可选） |
| `timestamp` | string | 错误发生时间（ISO 8601 格式） |
| `path` | string | 请求路径 |
| `traceId` | string | 追踪 ID，用于问题排查 |
| `stack` | string | 调用栈（仅开发环境返回） |

> **注意**：`code` 为遗留错误码格式（如 `AUTH_001`），`errorCode` 为新数字错误码格式（如 `2001`）。新代码应优先使用 `errorCode`，`code` 用于向后兼容。

---

## 2. 错误码分类说明

### 2.1 系统级错误 (1xxx)

系统级错误表示服务器内部问题或基础请求错误，通常由框架层或中间件产生。

| 错误码范围 | 分类 | 说明 |
|-----------|------|------|
| 1000 | 未知错误 | 未分类的服务器内部错误 |
| 1001 | 请求参数错误 | 请求格式不正确或缺少必要参数 |
| 1002 | 未授权访问 | 未登录或 Token 无效 |
| 1003 | 权限不足 | 已登录但无操作权限 |
| 1004 | 资源不存在 | 请求的资源未找到 |
| 1005 | 资源冲突 | 资源已存在或状态冲突 |
| 1006 | 参数校验失败 | class-validator 校验不通过 |

### 2.2 参数验证错误 (2xxx)

认证授权相关错误，表示身份验证或权限检查失败。

| 错误码范围 | 分类 | 说明 |
|-----------|------|------|
| 2001 | 凭证无效 | 用户名或密码错误 |
| 2002 | Token 过期 | Access Token 已过期 |
| 2003 | Token 无效 | Token 格式错误或签名不匹配 |
| 2004 | 尝试次数过多 | 登录或敏感操作尝试次数超限 |
| 2005 | 账户已锁定 | 登录失败次数过多导致账户锁定 |

### 2.3 业务逻辑错误 (3xxx)

业务逻辑错误表示操作违反业务规则或状态不正确。

| 错误码范围 | 分类 | 说明 |
|-----------|------|------|
| 3001 | 余额不足 | 会员卡余额或账户余额不足 |
| 3002 | 无效状态转换 | 状态机不允许的状态转换 |
| 3003 | 重复条目 | 唯一性约束冲突 |
| 3004 | 资源所有权违规 | 访问不属于当前诊所/用户的资源 |

### 2.4 数据完整性错误 (4xxx)

数据层错误，表示数据库约束或数据一致性问题。

| 错误码范围 | 分类 | 说明 |
|-----------|------|------|
| 4001 | 数据完整性错误 | 数据库约束违反 |
| 4002 | 引用不存在 | 外键关联的记录不存在 |

### 2.5 遗留错误码分类

除了数字错误码外，系统还保留了字符串格式的遗留错误码：

| 前缀 | 分类 | 示例 |
|------|------|------|
| `AUTH_xxx` | 认证授权错误 | `AUTH_001` - 未授权 |
| `GEN_xxx` | 通用错误 | `GEN_001` - 参数校验失败 |
| `PATIENT_xxx` | 患者相关错误 | `PATIENT_001` - 患者不存在 |
| `CHARGE_xxx` | 收费相关错误 | `CHARGE_001` - 收费单不存在 |
| `INVENTORY_xxx` | 库存相关错误 | `INVENTORY_001` - 库存不足 |
| `CARD_xxx` | 会员卡相关错误 | `CARD_001` - 会员卡不存在 |
| `REFUND_xxx` | 退款相关错误 | `REFUND_001` - 退款金额超过已付金额 |
| `BIZ_xxx` | 业务操作错误 | `BIZ_001` - 业务错误 |
| `DB_xxx` | 数据库错误 | `DB_001` - 数据库错误 |
| `REQ_xxx` | 请求相关错误 | `REQ_001` - 请求体过大 |
| `RATE_xxx` | 限流错误 | `RATE_001` - 请求过于频繁 |

---

## 3. 完整错误码列表

### 3.1 数字错误码列表

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| **1000** | 服务器内部错误 | 500 | 未知错误 | 未捕获的异常、未分类的错误 |
| **1001** | 请求参数错误 | 400 | 坏请求 | 请求格式错误、缺少必要参数 |
| **1002** | 未授权访问 | 401 | 未认证 | 未登录、Token 无效、Token 过期 |
| **1003** | 权限不足 | 403 | 无权限 | 角色不匹配、资源所有权违规 |
| **1004** | 资源不存在 | 404 | 未找到 | 请求的记录不存在、路由不存在 |
| **1005** | 资源冲突 | 409 | 冲突 | 唯一约束违反、状态冲突 |
| **1006** | 参数校验失败 | 400 | 校验失败 | class-validator DTO 校验不通过 |
| **2001** | 用户名或密码错误 | 401 | 登录失败 | 用户名不存在或密码不匹配 |
| **2002** | 令牌已过期 | 401 | Token 过期 | Access Token 超过有效期 |
| **2003** | 令牌无效 | 401 | Token 无效 | Token 格式错误、签名不匹配、被吊销 |
| **2004** | 尝试次数过多 | 429 | 限流 | 登录尝试次数超限、接口调用过频 |
| **2005** | 账户已锁定 | 401 | 账户锁定 | 登录失败次数过多，账户临时锁定 |
| **3001** | 余额不足 | 400 | 余额不足 | 会员卡消费时余额不够 |
| **3002** | 无效的状态转换 | 400 | 状态机错误 | 尝试执行不允许的状态转换 |
| **3003** | 重复条目 | 409 | 唯一性冲突 | 创建重复记录 |
| **3004** | 资源所有权违规 | 403 | 越权访问 | 尝试访问其他诊所的数据 |
| **4001** | 数据完整性错误 | 500/400 | 数据库约束 | SQL 约束违反（唯一键、外键、检查约束） |
| **4002** | 引用不存在 | 400 | 外键错误 | 关联记录不存在 |

### 3.2 遗留错误码列表

#### 认证授权 (AUTH_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `AUTH_001` | 未授权 | 401 | 未登录 | 请求需要认证的接口但未提供 Token |
| `AUTH_002` | Token 已过期 | 401 | Token 过期 | Access Token 超过有效期 |
| `AUTH_003` | Token 无效 | 401 | Token 无效 | Token 格式错误或签名不匹配 |
| `AUTH_004` | 登录失败 | 401 | 登录失败 | 用户名或密码错误 |
| `AUTH_005` | 密码错误 | 401 | 密码错误 | 修改密码时原密码不正确 |
| `AUTH_006` | 账户已禁用 | 401 | 账户禁用 | 用户账户被管理员禁用 |
| `AUTH_007` | 账户已锁定 | 401 | 账户锁定 | 登录失败次数过多 |
| `AUTH_008` | 权限不足 | 403 | 无权限 | 角色不满足要求 |

#### 通用错误 (GEN_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `GEN_001` | 参数校验失败 | 400 | 校验失败 | DTO 校验不通过 |
| `GEN_002` | 缺少必填字段 | 400 | 字段缺失 | 必要参数未提供 |
| `GEN_003` | 格式不正确 | 400 | 格式错误 | 参数格式不符合要求 |
| `GEN_004` | 资源不存在 | 404 | 未找到 | 通用资源不存在 |
| `GEN_005` | 用户不存在 | 404 | 用户不存在 | 操作的用户记录不存在 |
| `GEN_006` | 患者不存在 | 404 | 患者不存在 | 操作的患者记录不存在 |
| `GEN_007` | 资源不存在 | 404 | 资源不存在 | 通用资源未找到 |
| `GEN_008` | 资源冲突 | 409 | 冲突 | 通用资源冲突 |
| `GEN_009` | 唯一约束冲突 | 409 | 唯一键冲突 | 数据库唯一约束违反 |
| `GEN_010` | 重复操作 | 409 | 重复 | 重复执行相同操作 |
| `GEN_011` | 重复条目 | 409 | 重复记录 | 创建重复数据 |
| `GEN_012` | 内部错误 | 500 | 内部错误 | 通用服务器错误 |

#### 数据库错误 (DB_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `DB_001` | 数据库错误 | 500 | 数据库错误 | 未分类的数据库错误 |
| `DB_002` | 关联数据不存在 | 400 | 外键约束 | FOREIGN KEY constraint failed |
| `DB_003` | 数据不满足校验条件 | 400 | 检查约束 | CHECK constraint failed |
| `DB_004` | 必填字段不能为空 | 400 | 非空约束 | NOT NULL constraint failed |
| `DB_005` | 数据库繁忙 | 503 | 数据库忙 | SQLITE_BUSY，写入冲突 |
| `DB_006` | 数据被锁定 | 409 | 表被锁定 | SQLITE_LOCKED |
| `DB_007` | 数据库损坏 | 500 | 数据库损坏 | database disk image is malformed |
| `DB_008` | 数据库只读 | 503 | 只读模式 | SQLITE_READONLY |
| `DB_009` | 数据库读写错误 | 500 | IO 错误 | SQLITE_IOERR |

#### 请求错误 (REQ_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `REQ_001` | 请求体过大 | 413 | Payload Too Large | 请求超过大小限制 |
| `REQ_002` | 请求参数错误 | 400 | 坏请求 | 通用请求错误 |
| `REQ_003` | 不支持的媒体类型 | 415 | Unsupported Media Type | Content-Type 不正确 |

#### 限流错误 (RATE_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `RATE_001` | 请求过于频繁 | 429 | 限流 | 超过速率限制 |

#### 患者相关 (PATIENT_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `PATIENT_001` | 患者不存在 | 404 | 患者不存在 | 查询或操作的患者不存在 |
| `PATIENT_002` | 患者已存在 | 409 | 患者重复 | 手机号/身份证号重复 |

#### 收费相关 (CHARGE_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `CHARGE_001` | 收费单不存在 | 404 | 收费单不存在 | 操作的收费记录不存在 |
| `CHARGE_002` | 已支付 | 409 | 重复支付 | 收费单已支付，不能再次支付 |
| `CHARGE_003` | 已取消 | 409 | 已取消 | 收费单已取消，不能支付 |
| `CHARGE_004` | 支付失败 | 400 | 支付失败 | 支付处理失败 |

#### 库存相关 (INVENTORY_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `INVENTORY_001` | 库存不足 | 400 | 库存不足 | 出库数量超过现有库存 |
| `INVENTORY_002` | 物品不存在 | 404 | 物品不存在 | 操作的库存物品不存在 |

#### 会员卡相关 (CARD_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `CARD_001` | 会员卡不存在 | 404 | 卡不存在 | 操作的会员卡不存在 |
| `CARD_002` | 余额不足 | 400 | 余额不足 | 消费金额超过卡内余额 |
| `CARD_003` | 会员卡已禁用 | 400 | 卡禁用 | 会员卡已被停用 |

#### 退款相关 (REFUND_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `REFUND_001` | 退款金额超过已付金额 | 400 | 超额退款 | 退款金额大于已支付金额 |
| `REFUND_002` | 退款已处理 | 409 | 重复退款 | 退款申请已处理 |

#### 业务操作 (BIZ_xxx)

| 错误码 | message | HTTP 状态码 | 说明 | 触发场景 |
|--------|---------|------------|------|---------|
| `BIZ_001` | 业务错误 | 400 | 通用业务错误 | 未分类的业务错误 |
| `BIZ_002` | 操作失败 | 400 | 操作失败 | 业务操作执行失败 |
| `BIZ_003` | 备份失败 | 500 | 备份失败 | 数据库备份失败 |
| `BIZ_004` | 恢复失败 | 500 | 恢复失败 | 数据库恢复失败 |
| `BIZ_005` | 配置错误 | 500 | 配置错误 | 系统配置不正确 |
| `BIZ_006` | 数据库错误 | 500 | 数据库错误 | 数据库操作异常 |

---

## 4. 前端处理建议

### 4.1 统一错误处理

建议在前端 HTTP 客户端（如 axios 拦截器）中统一处理错误：

```typescript
// axios 响应拦截器示例
axios.interceptors.response.use(
  (response) => response,
  (error) => {
    const { response } = error;
    
    if (!response) {
      // 网络错误
      message.error('网络连接失败，请检查网络');
      return Promise.reject(error);
    }

    const { status, data } = response;
    const errorCode = data?.errorCode;
    const message = data?.message || '请求失败';

    // 401: 未授权，跳转到登录页
    if (status === 401) {
      // 清除本地 Token
      localStorage.removeItem('token');
      // 跳转到登录页
      window.location.href = '/login';
      return Promise.reject(error);
    }

    // 403: 权限不足
    if (status === 403) {
      message.error('您没有权限执行此操作');
      return Promise.reject(error);
    }

    // 429: 请求限流
    if (status === 429) {
      const retryAfter = response.headers['retry-after'];
      message.warning(`请求过于频繁，请 ${retryAfter || '稍后'} 秒后重试`);
      return Promise.reject(error);
    }

    // 400: 参数校验错误，展示详细错误
    if (status === 400 && data?.errors) {
      const errors = data.errors.join('\n');
      message.error(errors);
      return Promise.reject(error);
    }

    // 其他错误展示 message
    message.error(message);
    return Promise.reject(error);
  }
);
```

### 4.2 按错误码分类处理

| 错误码 | 前端处理方式 |
|--------|-------------|
| 1002 / 2002 / 2003 | 清除 Token，跳转登录页 |
| 1003 / 3004 | 提示"权限不足"，禁止操作 |
| 1004 | 提示"资源不存在"，返回列表页 |
| 1005 / 3003 | 提示"数据已存在"，刷新页面 |
| 1006 | 展示字段级错误信息 |
| 2004 / 2005 | 显示倒计时，锁定期间禁用登录按钮 |
| 429 | 显示重试倒计时，禁用提交按钮 |

### 4.3 traceId 用于问题排查

当用户反馈问题时，应收集 `traceId` 以便后端快速定位问题：

- 在错误提示中包含 traceId（可选，面向技术用户）
- 在前端错误日志中记录 traceId
- 提供"复制错误信息"功能，包含 traceId

---

## 5. 错误码扩展规范

### 5.1 新增错误码原则

1. **优先使用现有错误码**：不要为相似场景创建新错误码
2. **分类明确**：新错误码必须归入正确的分类区间
3. **粒度适中**：不要过粗（一个错误码涵盖所有情况）也不要过细（每个字段一个错误码）
4. **消息友好**：错误信息应让用户能理解并知道如何处理

### 5.2 数字错误码命名规范

| 区间 | 分类 | 示例 |
|------|------|------|
| 1xxx | 系统/HTTP 层 | 1000 - 1099 |
| 2xxx | 认证授权 | 2000 - 2099 |
| 3xxx | 业务逻辑 | 3000 - 3999 |
| 4xxx | 数据/资源 | 4000 - 4999 |
| 5xxx | 第三方服务 | 5000 - 5999 |

### 5.3 遗留错误码命名规范

遗留字符串错误码格式：`{PREFIX}_{NNN}`

- **前缀**：3-5 个大写字母，表示业务模块
- **序号**：3 位数字，从 001 开始递增

常用前缀：
- `AUTH` - 认证授权
- `GEN` - 通用
- `PATIENT` - 患者
- `CHARGE` - 收费
- `INVENTORY` - 库存
- `CARD` - 会员卡
- `REFUND` - 退款
- `BIZ` - 业务
- `DB` - 数据库
- `REQ` - 请求
- `RATE` - 限流

### 5.4 添加新错误码步骤

1. 在 `src/common/errors/error-codes.ts` 中添加：
   - `ErrorCode` 枚举中添加新错误码
   - `errorMessages` 中添加对应消息
   - 如需遗留格式，在 `LegacyErrorCode` 中也添加

2. 在业务代码中抛出异常时使用：
   ```typescript
   throw new BusinessValidationException(
     '自定义错误消息',
     ErrorCode.NEW_ERROR_CODE
   );
   ```

3. 更新本文档，添加新错误码的说明和触发场景

### 5.5 错误码迁移计划

当前系统处于新旧错误码并存阶段，未来迁移计划：

- **阶段 1**（当前）：同时返回 `code`（字符串）和 `errorCode`（数字）
- **阶段 2**：新接口仅返回 `errorCode`，保留 `code` 用于兼容旧接口
- **阶段 3**：完全迁移到数字错误码，移除字符串格式

> 新开发的接口应优先使用数字错误码，前端新代码也应优先读取 `errorCode` 字段。

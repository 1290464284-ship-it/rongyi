# 深度审计报告 — 第三轮（前端 UI 层）

- 审计对象：`D:/Desktop/rongyi/source/apps/v2`（前端：React 19 + Vite，Electron 桌面壳）
- 审计范围（git log 从旧到新）：`49e6770` Token 换肤+暗色主题 → `d706c3e` Dialog 开合动画状态机 → `269e40a` 拆分 7 个超长页面 → `3bfb27f` 修复 10 处 lint → `23ba772` 登录页居中改版+记住我 → `6ee132f` Layout 顶栏/侧栏/全局搜索/统计卡/区块标题
- 方法：逐文件静态核对 + `git show` 多节点 diff 对比（拆分前 `269e40a^` / 拆分时 `269e40a` / 修复时 `3bfb27f` / 当前 HEAD）+ 自写脚本实测（CSS 类失配扫描、WCAG 对比度计算）；**全部发现均亲眼验证**，未做任何修改
- 结论速览：**高 1 项（3bfb27f 修复引入的数据清空回归）、中 4 项、低 4 项、提示 5 项**；上一轮 35+ CSS 失配与 68799bd 声称修复项全部验证落地

---

## 一、上轮问题验证结果

### 1.1 已修（验证落地 ✓）

| 上轮项 | 验证结果 | 证据 |
|---|---|---|
| CSS 反向失配 35+ 类 | **已补齐**。自写脚本扫描：146 个已定义 CSS 类 vs 136 个使用类，仅剩 2 个"用了没样式"且均无视觉影响 | `login-panel`（LoginPage.tsx:65）、`sidebar-group-wrap`（Layout.tsx:147）；`table-empty`/`drag-over` 等缺失类均已定义 |
| ResourcePage 无防抖 | 已修：`useDebouncedValue(300)` | ResourcePage.tsx:63（300ms 防抖） |
| ResourcePage datetime 直传 | 已修：`toLocalInput` 转本地输入格式 | ResourcePage.tsx:53 |
| RelationSelect 加载累积 | 已修：替换式累积 | FormBuilder RelationSelect :95-114 |
| 删除末页回退 | 已修：删除后空页回退一页 | use-crud-resource.ts:217-219 |
| ResourceHub tab 保持挂载 | 已修，且 6ee132f 未回退 | ResourceHub tab 切换不卸载 + aria 已加 |
| 全局 ToastProvider | 已确认 | main.tsx 全局挂载 |
| prefers-reduced-motion | 已加 | styles.css:1723 `@media (prefers-reduced-motion: reduce)` |
| React StrictMode 兼容 | 无碍（Dialog effect 幂等、无重复提交） | components.tsx:237-254 |

### 1.2 部分验证

- **68799bd 防抖/回填/累积抽查**：全部确认落地（见上表），**但** ProcessingOrdersPage 编辑回填的竞态结构在拆分前已存在（见 2.6，非本轮回归）。

---

## 二、新发现（按严重度）

### 【高】H1 — 回归：ReportDialog 只改颜色后保存会清空全部报告数据（3bfb27f 引入）

- **位置**：`src/web/cephalometric/ReportDialog.tsx:52`（配合 :22/:33/:49-61）
- **证据（三节点 diff 对比）**：
  - 拆分前 `269e40a^` 与拆分时 `269e40a` 的 ReportDialog：`jsonText` 由 `useEffect` 在报告数据加载后**回填**（`setJsonText(JSON.stringify(report, null, 2))` + `hydrated` 守卫），保存前 `jsonText` 必然已含服务端完整数据；
  - `3bfb27f` 把 effect 回填改为**渲染派生**（`jsonText=null` 表示未编辑，显示时用 `effectiveJsonText` 派生，:33），但 `handleSave` 仍写 `JSON.parse(jsonText || '{}')`（:52）——未编辑 JSON 时 `jsonText === null` → 解析 `'{}'` → 空对象；
  - 保存载荷 `{ ...parsed, outlineColor, lineColor }`（:61）→ 服务端 `reportJson` 被替换为只剩两个颜色字段的**空对象**。
- **触发路径**：打开已有报告 → 只改"轮廓色/折线色"（不动 JSON 文本域）→ 保存 → `outline/polylines/conclusion` 全部清空（服务端永久丢失，无撤销）。
- **影响**：这是 3bfb27f"行为等价"声明下**实际不等价**的典型；改动颜色是高频操作，报告轮廓数据不可恢复。修复方向：`handleSave` 改用 `effectiveJsonText` 解析（一行修复）。

### 【中】M1 — 新问题：暗色主题对比度不足（49e6770 引入）

- **位置**：`styles.css:75-101` 暗色 token 块（`--primary: #0E8A8A` :80、`--border-strong: #2E4A50` :92、`--muted-weak: #5F7A82` :95）+ 亮色块（`--muted-weak: #8FA3A8` :27、`--warning: #D9822B` :32、`--warning-soft: #FBF0E4` :33）
- **证据（WCAG 相对亮度实测）**：
  - 暗色 `--primary #0E8A8A` 用于 13px 文字（sidebar nav active、role-badge、.info、链接）：on `--surface #132124` = **3.95:1**、on `--surface-soft #163238` = **3.24:1**、on `--surface-muted #16262A` = **3.73:1**——全部 < 4.5（仅达 AA-large）；
  - 暗色 `--border-strong #2E4A50` vs `#132124` = **1.74:1**（非文本 3:1 不达标，输入框/表格/卡片边界在暗色下几乎不可见）；亮色同样 1.51:1；
  - 亮色 `--muted-weak #8FA3A8` on `#F5F9F8` = **2.48:1**（placeholder、sidebar-group 分组名、表格 muted 小字 FAIL）；
  - 亮色 `--warning #D9822B` on `--warning-soft #FBF0E4` = **2.60:1**（`tag.nudge` 标签文字 FAIL）；暗色对应项（#E8A24C on #3A2A14 = 6.38）达标；
  - 主按钮白字 on `#0E8A8A` = **4.18:1**（< 4.5，13px 文字仅 AA-large）。
- **影响**：暗色主题下导航激活态、徽标、说明性文字与边界辨识度低；亮色下 placeholder/警示标签不达 AA。属新主题功能的可用性问题，非崩溃级。

### 【中】M2 — 新问题：Dialog"统一关闭出口"未真正统一，取消/保存按钮无关闭动画（d706c3e 引入）

- **位置**：`components.tsx:259-266`（`requestClose` 唯一延迟出口）；调用方只有两处——Escape（:271）与遮罩 mouseDown（:300）；而取消按钮直连：`components.tsx:344`（ConfirmDialog 取消 → `onCancel`）、`:397`（PromptDialog 同理）、`CrudPage.tsx:109`、以及各页面 Dialog 子内容里的"取消"按钮（ChargeDialog.tsx:118、RecordDialog.tsx:105、TriageDialog.tsx:90、PlanBillingDialog.tsx:201、DispenseNarcoticPanel.tsx:341 等全部直连 onClose）；保存成功路径同样直接关（use-crud-resource.ts:183）。
- **证据**：提交信息声称"Escape/遮罩/取消统一出口"；实测代码中取消按钮**不经过** `requestClose` → 父级 `setState(false)` → 组件立即卸载，120ms 淡出动画不播放。`components.spec.tsx` 新增用例只覆盖 Escape/backdrop 两条延时路径（断言在 `advanceTimersByTime(150)` 之后才检查 `onClose` 被调用，取消按钮即使同步调用也会通过，无法暴露差异）。
- **影响**：按 Escape 弹窗延迟 140ms 淡出，点取消瞬间消失——同一操作两种视觉行为；动画功能覆盖面与设计意图不符。

### 【中】M3 — 新问题：closing 动画期间点"新建"操作被吞（d706c3e 引入）

- **位置**：`components.tsx:259-266`（`requestClose` 延迟 140ms 后才 `onClose`）+ `use-crud-resource.ts:127-131`（`openCreate` 重置表单并 `setShowForm(true)`）+ `CrudPage.tsx:81/:105`
- **证据**：用户按 Escape/点遮罩后弹窗进入 `closing` 但父组件 `open` 仍为 true；140ms 内若点击"新建"→ `openCreate` 重置表单、显示弹窗 → 紧接着旧 timer 触发 `onClose` → `closeForm` → `showForm=false`，**用户的打开动作被静默吞掉**（表单已重置、弹窗又关闭）。另注意关闭动画期间 backdrop 处于 `pointer-events` 受控态，点击可穿透到页面按钮（动画播完前用户即可触发）。
- **影响**：低概率（140ms 窗口）但表现为"点了没反应"+表单重置，交互确定性问题；修复方向：`onClose` 回调前校验/取消防抖，或 `requestClose` 期间忽略新的 open 请求。

### 【中】M4 — 新问题：拆分新建的 SVG 组件硬编码浅色，暗色主题下刺眼白底（269e40a 引入）

- **位置**：`cephalometric/OutlineSvg.tsx:21-24`、`cephalometric/CompareResultView.tsx:23-26`
- **证据**：两处均内联 `background: '#f8fafc'`、`border: '1px solid #c9d3de'`、空态文字 `fill: '#6b7280'`——与 token 体系（styles.css 亮/暗双主题）完全脱钩。
- **影响**：暗色主题下头颅侧位轮廓图/对比图为高亮白底方块，视觉突兀（其余硬编码如 logo 圆点 #fff、btn-login #fff、rgba 渐变阴影可接受——单色装饰）。
- **修复方向**：改用 `var(--surface-soft)` / `var(--border-strong)` / `var(--muted-weak)` 或 CSS 类。

### 【低】L1 — 新问题：条件渲染型 Dialog 关闭动画完全不生效（d706c3e 与既有模式冲突）

- **位置**：`DispenseListPanel.tsx:117-123`、`CephalometricPage.tsx:152-157`（DataTable 行 td 内嵌）、`ClinicalWorkflowPage.tsx:143-154`、`FirstExamsPage.tsx:99-125`、`SendWechatDialog`/`ReportDialog`/`HistoryDialog`/`TeethMarkDialog`/`TrackingDialog`（均为 `<Dialog open>` 恒 true、父级条件卸载）
- **证据**：关闭动画依赖 `closing` 状态 + 父级延迟卸载；条件渲染场景父级直接不渲染子组件，`closing` 无从播放，弹窗立即消失。CephalometricPage/FirstExamsPage 的 Dialog 在每行 td 内实例化（position:fixed 可用，但整表行组件数量膨胀）。
- **影响**：动画功能在这些高频弹窗上无效果；行为不一致面进一步扩大（与 M2 同类，覆盖面差异）。

### 【低】L2 — 新问题：全局搜索 placeholder 承诺"患者/项目/单号"但只搜患者（6ee132f 引入）

- **位置**：`Layout.tsx:82-87`（提交式跳转 `/patients?q=`）；`PatientsPage.tsx` 用 `key={urlSearch}` 强制重挂载处理重复搜索（✓ 二次搜索已修复）
- **证据**：placeholder 声称"搜索患者/项目/单号"，实际只跳患者页；无独立无结果态（复用表格空态）；顶栏 nav 无显式 aria-label（隐式 landmark，可接受）。
- **影响**：文案与行为不符，误导用户；功能范围问题而非缺陷。

### 【低】L3 — 新问题：登录页改版+记住我无测试覆盖（23ba772 引入）

- **位置**：`src/web/LoginPage.spec.tsx`
- **证据**：`git log -- LoginPage.spec.tsx` 最后一次修改为 `4a9e2af`（改版前）；`23ba772` 仅改 LoginPage.tsx + styles.css，未同步测试。同期 `Layout.spec.tsx` 已在 6ee132f 更新（+19，适配新品牌名"蓉易口腔诊所"与 `/me` mock）、`components.spec.tsx`/`ResourcePage.spec.tsx` 已在 d706c3e 更新 ✓。
- **影响**：记住我读写、居中改版、登录失败路径无回归保护；与仓库"页面 spec 全覆盖"惯例不符。

### 【低】L4 — 新问题：Dialog 打开动画与 focus 时序（d706c3e 引入，轻微）

- **位置**：`components.tsx:237-254`
- **证据**：`useEffect` 在 `open` 为 true 时 `focus()` 首个可聚焦元素；150ms 开启动画期间元素已聚焦（无问题）；但 `previouslyFocused` 记录于 effect 内、`requestClose` 140ms 后卸载时还原焦点（:251）——若用户在此期间已点击页面其他元素，焦点会被强行拉回触发元素（140ms 窗口内的强制聚焦）。影响极小，属可接受的 UX 权衡，仅记录。

### 【提示】P1 — ProcessingOrderFormFields 明细回填竞态（**既有问题延续，非本轮回归**）

- **位置**：`processing-orders/ProcessingOrderFormFields.tsx:26-52`
- **证据**：`git show 269e40a^` 确认该 effect（`[editing, editingId, update]` 依赖 + `cancelled` cleanup + `loadedItemsForRef` 守卫）在拆分前已**逐字相同**存在（旧 ProcessingOrdersPage.tsx:517-543），3bfb27f 仅删除未用 import。`update` 每次父渲染都是新引用（use-crud-resource.ts:123 普通函数）→ 父级任意重渲染（用户输入、react-query 后台刷新）→ cleanup 置 `cancelled=true` → 在途请求结果被丢弃；`loadedItemsForRef` 守卫又阻止重试（同 ID 重开也不再加载）。
- **影响**：本地 API 快时窗口小，但触发后明细永不回填（`form.items` 保持空行）；此时若直接保存，`reconcileProcessingItems`（items.ts:89-93）会把服务端明细当作"被移除的行"全部 DELETE——**UI 侧数据丢失链路**。加载失败后同 ID 重开不重试（ref guard）。建议：依赖改为 `[editing, editingId]` + ref 保存 update，或去掉 ref 守卫、失败/取消后可重试。

### 【提示】P2 — ChargesPage 删除按钮无响应（既有问题，拆分前后一致）

- **位置**：`ChargesPage.tsx:125`（`onDelete={setDeleteTarget}`）+ `:252-264`（`deleteCharge` 函数）+ `charges/ChargeList.tsx:29`（删除按钮）
- **证据**：点击删除只设置 `deleteTarget` 状态；页面**未渲染任何 ConfirmDialog**、`deleteCharge` 无人调用（拆分前旧文件同样无 ConfirmDialog 渲染）。拆分属纯搬运，未修未引入。

### 【提示】P3 — 首诊/加工非受控 select defaultValue 不重置（既有）

- **位置**：`ProcessingOrdersPage.tsx:254`、`FirstExamsPage.tsx:71`
- **证据**：非受控 `select defaultValue`，同一条记录重开编辑弹窗不回显/不重置（旧行为延续）。

### 【提示】P4 — 工作台 FollowUpDialog 预填日期用 UTC（既有）

- **位置**：`clinical-workflow/FollowUpDialog.tsx:21`：`new Date(Date.now() + 7*86_400_000).toISOString().slice(0,10)`
- **证据**：`toISOString()` 按 UTC 截日期，东八区 00:00–08:00 生成的"一周后"会差一天；`format.ts` 已有 `todayLocalDate()` 但此处未用（延续"今天三套实现"旧账）。

### 【提示】P5 — 拆分时 PlanFormFields/DispenseEditDialog 的渲染派生改动属等价重构（验证通过）

- **位置**：`treatment-plans/PlanFormFields.tsx`、`dispense/DispenseEditDialog.tsx`（3bfb27f 同批改为渲染派生）
- **证据**：`git show 3bfb27f` 比对——PlanFormFields 的 `itemsLoading` 改派生（`editing && planId && !itemsLoaded`）、DispenseEditDialog 回填改派生（`null=未编辑`），保存路径均使用派生值（与 ReportDialog 不同，这两处保存读的是派生文本，无 H1 类问题）；`plan-utils.ts` 的 `updatePlanWithItems` reconcile（billed 保护：不 PATCH/不 DELETE 已划价明细 ✓）、`cleanupOrphanPlan` 先删明细再删主记录 ✓、`buildValidItems` 丢弃提示（ChargeCreateForm.tsx:30-35 的 filled-valid 差值 toast）行为与拆分前一致。

---

## 三、发现分类汇总

### 修复引入的回归（fix 提交把原本正确的行为改坏了）
1. **H1** ReportDialog 保存清空报告数据 —— `3bfb27f` 将 effect 回填改为渲染派生，但 `handleSave` 仍解析原始 `jsonText`（null → `'{}'`）。

### 新变更引入的新问题（新功能/重构自身带来的问题）
2. **M1** 暗色主题对比度不足（49e6770 token）
3. **M2** Dialog 关闭出口不统一，取消/保存无动画（d706c3e，与提交说明不符）
4. **M3** closing 动画期间点"新建"操作被吞（d706c3e）
5. **M4** OutlineSvg/CompareResultView 硬编码浅色（269e40a 新建文件）
6. **L1** 条件渲染 Dialog 无关闭动画（d706c3e 功能覆盖不全）
7. **L2** 全局搜索 placeholder 与行为不符（6ee132f）
8. **L3** LoginPage.spec.tsx 未随 23ba772 更新（测试缺口）
9. **L4** closing 期间焦点强制还原（d706c3e，轻微）

### 既有问题延续（本轮确认存在，非新引入；拆分/修复未改变行为）
- P1 ProcessingOrderFormFields 回填竞态+ref 守卫不重试（拆分前逐字相同，但风险链建议本轮处理）
- P2 ChargesPage 删除无响应
- P3 非受控 select defaultValue 不重置
- P4 FollowUpDialog UTC 日期
- CephalometricPage/FirstExamsPage 行内 Dialog 每行实例化（拆分搬运）

### 已验证无问题的点
- 上轮 35+ CSS 类补齐 ✓；68799bd 防抖/回填/累积 ✓；删除末页回退 ✓；remember me 键无冲突、密码不落 localStorage、隐私模式 try/catch ✓；Dialog 焦点管理/aria ✓；react-query key 隔离 ✓；6ee132f `initialSearch` 仅影响 PatientsPage ✓。

---

## 四、5 条最值得修的问题（排序）

1. **【H1】ReportDialog 保存清空报告数据**（`cephalometric/ReportDialog.tsx:52`）：把 `JSON.parse(jsonText || '{}')` 改为解析 `effectiveJsonText`。一行修复，消除唯一高危数据丢失；补一条"只改颜色保存后轮廓仍在"的 spec。
2. **【P1】ProcessingOrderFormFields 回填竞态+数据删除链路**（`ProcessingOrderFormFields.tsx:26-52`）：依赖改为 `[editing, editingId]`（update 用 ref 转发），并允许失败/取消后重试；同时给保存前加"明细为空则提示"守卫，切断"回填失败→保存→DELETE 服务端明细"的连锁。
3. **【M1】暗色主题对比度**（`styles.css:80-101`）：暗色 `--primary` 提亮至 ≥4.5:1（如 #14A5A5 系）、`--border-strong` 提亮至 ≥3:1（如 #3E6068）；亮色 `--muted-weak`/`--warning` 同步加深。用脚本回归对比度（本轮已有 contrast.mjs 可复用）。
4. **【M2+M3】Dialog 关闭出口统一与竞态**（`components.tsx`）：取消按钮/保存成功路径改走 `requestClose`（或 Dialog 提供 `close()` 方法）；`closing` 期间忽略新的 `open` 请求（或 onClose 后置校验）；补取消按钮延时路径测试（先在 `advanceTimers` 前断言 `onClose` 未调用，即可暴露现状）。
5. **【M4+L1】主题脱钩的硬编码与动画覆盖**（`OutlineSvg.tsx:21`、`CompareResultView.tsx:23`）：SVG 背景/边框/文字改用 CSS 变量；条件渲染 Dialog 迁移为受控 `open` + 父级延迟卸载（统一出口后自然覆盖）。

---

### 附：验证方法记录
- 自写脚本：`css-scan.mjs`（146 CSS 类 vs 136 使用类，模板字符串感知）、`contrast.mjs`（WCAG 相对亮度/对比度，逐 token 组合实测）；
- git 三节点对比：`269e40a^`（拆分前）→ `269e40a`（拆分时）→ `3bfb27f`（lint 修复）→ HEAD，确认 H1 为 3bfb27f 引入、P1/P2 为拆分前既有；
- 测试文件 git log 交叉验证：LoginPage.spec.tsx 停在 4a9e2af（未随 23ba772 更新），Layout.spec.tsx/components.spec.tsx/ResourcePage.spec.tsx 已随本轮更新。

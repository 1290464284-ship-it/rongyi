# Wave 3 进度跟踪（2026-08-05）

来源：docs/plans/2026-08-05-audit-round2.md（Wave 3 节）+ docs/plans/2026-08-05-audit-fixes.md（Phase 4）。
流程：子代理实现 → 调用方审 diff → 全量复验 → 独立提交。

## Round A（并行 5）✅ 全部提交
- [x] T2R-11 通知/微信角色收窄（route-policy.ts + resources.ts）→ `18814e2`
- [x] T2R-12 errorMiddleware details 收敛（middleware.ts）→ `0a9e462`
- [x] T2R-16 FTS 子查询 clinicId 过滤（repository.ts）→ `ed457b7`（调用方修正：字面 clinicId=? 触发 architecture.spec 守卫，改复用 tenantAnd；tenantAnd 自带 AND 前缀不可再 slice）
- [x] T4.7 datetime 输入归一化（validation.ts）→ `ae398c4`
- [x] T4.11 金额溢出防护（financial.ts + services.spec.ts）→ `e2d41d6`（535 passed）

## Round B（并行 4）✅ 全部提交
- [x] T4.1 sync pull 游标 + push 事务批（sync.ts + services.spec.ts）→ `e8a9eb9`（偏离：db.transaction 拒 async，用显式 BEGIN/COMMIT/ROLLBACK，idempotency.ts 有先例）
- [x] T4.2 预约看板本地日期（read-routes.ts + app.ts deps 一行 + AppointmentBoardPage.tsx）→ `7ab0879`（阻塞点：read-routes 无 db，方案 A 批准接线）
- [x] T4.6 logger Error 序列化 + 文案翻译（logger.ts + messages.ts）→ `4cfc039`（计划 7 条翻译 5 条已存在，仅补 2 条）
- [x] T2R-15 64KB 导入启发式修正（main.ts）→ `94d64d8`（shouldImportLegacyDb 纯函数 + quick_check）

## Round C（并行 3）✅ 全部提交
- [x] T4.4 微信错误明细 + 批量并发（wechat.ts + services.spec.ts）→ `2cfa3b6`（子代理停下请求授权后调用方直接收尾：2 处 toEqual→toMatchObject + app.ts wechat logger 接线；68/68）
- [x] T4.5 登录审计 + 请求日志 userId（app.ts + auth-admin.ts）→ `24a4930`（AuditInput 顶层导出 + app.locals.audit；与 T4.4 共用 app.ts，按 hunk 拆分提交：y/y/n/y）
- [x] T2R-13 API 子进程孤儿化防护（main.cjs + main.ts）→ `7e0aa43`（心跳 2s ping + 10s 超时 + ppid 探活；脚本级 19/19，验证套件在 Temp/t2r13-verify）

## Round D（并行 3）✅ 全部提交
- [x] T4.8 幂等覆盖扩展（router.ts + workflow.ts）→ `52b8fac`（通用 POST + 7 个 workflow 端点；async service 走 async 闭包、sync service 走事务路径；router.spec +1、app.spec +1）
- [x] T4.9 批量导入系统性错误分类（clinical-ops.ts + services.spec.ts）→ `84e4b96`（isSystematicError + IMPORT_SYSTEM_ERROR 500 + 分片提示"前 N 条已导入"；**偏离**：repository.insert 补 await——async 方法不 await 则真实 SQLITE 错误成未处理 rejection，原实现吞错；services.spec +3）
- [x] T2R-14 健康检查两级策略（main.cjs）→ `654a29b`（首启 30s / 运行中 2s；waitForApi 保留底层错误；api-health-check-failed 日志含 uptime/port/restartCount；apiLastHealthError 保留现场；vm 沙箱 18/18）

## Round E ✅ 提交
- [x] T4.10 审计 flush 重试与关闭冲刷（app.ts + main.ts + app.spec.ts）→ `9e8d57d`（_auditRetryScheduled 闭包级守卫恰好重试一次、容量守卫 MAX*2、unref 1s 重试；app.locals.flushAuditNow；main.ts IPC shutdown 先冲刷；app.spec +4 用例含恒失败有界性；568 passed）

## Wave 3 收官（2026-08-05）
全部 13 任务提交完毕：Round A 5 + Round B 4 + Round C 3 + Round D 3 + Round E 1 = 16 个 commit（含 T3.7 前序）。测试 523 → 568（+45）。
Wave 3 结束时本跟踪文件入库为 docs 提交。

## 提交记录
- 15ee1f0 T3.7（Wave 2 收官）
- 18814e2 / 0a9e462 / ed457b7 / ae398c4 / e2d41d6（Round A）
- e8a9eb9 / 7ab0879 / 4cfc039 / 94d64d8（Round B）
- 24a4930(T4.5) / 2cfa3b6(T4.4) / 7e0aa43(T2R-13)（Round C）
- 52b8fac(T4.8) / 84e4b96(T4.9) / 654a29b(T2R-14)（Round D）

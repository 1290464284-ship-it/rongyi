# 艾登特风格功能增强 - 验证检查清单 (Checklist)

> 使用方法：每个任务完成后逐项打勾。所有 `[ ]` 必须全部打勾后才算通过阶段验收。

---

## Phase 1: 基础设施

### T1: 数据库迁移 v31/v32 ✅
- [x] 12 张新表（CephalometricLandmarkSet / CephalometricAnalysisRecord / CephalometricNormValue / DrugContraindication / PatientRiskScore / BusinessAlert / InventoryReplenishmentSuggestion / SatisfactionSurvey / StaffSchedule / StaffLeaveRequest / DataImportJob）全部通过 `PRAGMA table_info` 校验存在
- [x] 每张新表含字段：id / clinicId / createdAt / updatedAt / deletedAt（软删除字段）
- [x] clinicId 字段有索引：12 张新表均追加 idx_xxx_clinic_deleted 复合索引；自然键表有 UNIQUE(clinicId, naturalKey)
- [x] Visit 表新增 nextReminder TEXT 、summaryAutoGenerated INTEGER DEFAULT 0 列生效（schema + 增量迁移双处）
- [x] MedicalPhrase 新增 ownerId、pinOrder 列生效
- [x] migrations/index.ts CURRENT_VERSION = 32；新增 v31/v32 迁移、import、switch-case、migrationNames 齐全
- [x] 迁移 idempotent：v31 用 CREATE TABLE IF NOT EXISTS，v32 用 addColumnIfMissing；TEST_DB_MEMORY 模式初始化无报错
- [x] Settings 中 17 个智能功能独立开关（DEFAULT_CONFIG + SystemConfig）可通过接口读写，默认值合理：规则引擎 ON，头影测量/排班/加密 OFF

### T2: 每日 Cron 调度器通用化 ✅
- [x] DailySchedulerService 在 NestJS 启动日志中输出「[DailyScheduler] 调度器已启动，每日执行时间 HH:mm，已注册 N=5 个子任务」
- [x] 注册空的 5 个子任务占位（BusinessAlertDetector / InventoryReplenishment / RfmAnalysis / ChurnPrediction / DoctorPerfAnomaly），onModuleInit 无报错
- [x] Mock 时钟加速到 02:00 → 5 个子任务全部执行一次（execute 调用数 = 5）
- [x] 其中 1 个子任务抛异常 → 其余 4 个正常完成；进程不崩溃；失败计数 +1、未到 3 次 BusinessAlert 不提前写
- [x] 单个子任务连续失败 3 次 → BusinessAlert 出现 1 条 SCHEDULER_TASK_FAILURE / CRITICAL 记录；参数化 SQL 无字符串拼接
- [x] scheduler 无内存泄漏：onModuleDestroy 中 clearTimeout + clearInterval 两者都被调用，timer 句柄置 null

---

## Phase 2: 结构化病历增强

### T3: 病历智能摘要 + 下次就诊提醒 ✅
- [x] 5 种典型病例（根管/拔牙/洁牙/充填/冠修复）摘要生成单测通过，关键字命中
- [x] 医生手动修改 nextReminder 后 summaryAutoGenerated=0，下次保存不自动覆盖（UPDATE WHERE 条件拒绝写回）
- [x] summary 长度 ≤ 100 字，超长截断结尾加「…」
- [x] 空数据（无诊断/无治疗）不崩溃，返回「本次一般检查，未见特殊处理…」T0 兜底模板
- [x] 多个治疗（根管+洁牙）→ 取最小 14 天复诊间隔；Settings.aiMedicalSummaryEnabled=false 跳过
- [x] 审计日志写入 MEDICAL_SUMMARY_AUTO_GENERATED；Visit.findMany/findOne SELECT 字段含 summary/nextReminder/summaryAutoGenerated
- [ ] ClinicalPage 顶部 nextReminder 卡片显示：黄色背景、可编辑、保存后持久化（留后续前端任务）

### T4: 短语收藏夹 + 牙位联动 ✅
- [x] 医生 A 收藏短语 → 医生 B scope=MINE 查询为空（ownerId 隔离）；ALL 返回公共 + 我 own
- [x] 置顶 pinOrder 排序：PIN_FIRST 倒序 > useCount DESC；reorderPin 6 条批量事务；重复 favorite 仅置顶不重复建
- [x] Tooth 状态联动：recommendForTeeth 命中 DECAY → 龋洞充填；PERIODONTITIS → 牙周基础；matchReasons 含具体 (toothNumber Status)
- [x] 非 SOUND 牙未指定 toothNumbers → 自动取患者全部非 SOUND 匹配；空患者无牙记录返回 []，不崩
- [x] 搜索 keyword 模糊 name/content 过滤；scope=MINE 优先 ownerId=me
- [x] 12 条默认种子幂等；useCount/lastUsedAt 1 次事务 UPDATE；CURRENT_VERSION=39；25/25 + 25/25 回归通过

---

## Phase 3: 本地规则 AI 辅助

### T5: 处方配伍禁忌 ✅
- [x] 20 条禁忌组合单测（甲硝唑+酒精、头孢+乙醇、华法林+双氯芬酸、两 NSAIDs 叠加、孕期四环素、哺乳期甲硝唑、喹诺酮+抗酸、肝肾不全禁忌）全部命中预期 WARN/DANGER
- [x] 无禁忌「维生素C + 氯己定 → 返回空数组
- [x] DANGER 级未传 ignoreContraindicationIds → create() 抛异常；传入包含 → audit_log 写 PRESCRIPTION_CONTRAINDICATION_BLOCKED；确认后写 PRESCRIPTION_CONTRAINDICATION_IGNORED，含 contraindicationIds JSON
- [x] DrugContraindication 种子 111 条（≥要求 100 条超量完成）v34 迁移 + ruleId/level/appliesToJson/bidirectional/doseMinDailyMg + 2 索引；诊所独立计数；幂等 seed
- [x] 25 项大处方校验 < 50ms；aiContraindicationEnabled=false 立即跳过重校验直接返回空
- [x] 人群级规则：孕早/中、肾/肝损与处方对应命中/绕过 全部通过；PrescriptionsService 31 用例 0 回归

### T6: 患者风险评分 ✅
- [x] 4 组典型病例（健康儿童 0-29 低 / 龋高风险儿童 60-79 高 / 糖尿病+吸烟老年牙周 80+ 极高 / 种植体周炎）等级正确
- [x] 边界：0 / 29 / 30 / 59 / 60 / 79 / 80 七个阈值等级切换正确
- [x] 写 PatientRiskScore 表快照：含因子分解(C1-C7/P1-P7/I1-I8) JSON + dataSources + weightsOverride，可回溯
- [ ] 雷达图 UI 显示：3 指标 0-100 归一化颜色正确（留后续前端任务）
- [x] 因子权重可在 Settings 调（27个参数：aiRiskCaries*/aiRiskPeriodontal*/aiRiskImplant*）保存后下次计算生效
- [x] v35 迁移 PatientRiskScore(clinicId,patientId,createdAt DESC) 复合索引；PATIENT_RISK_SCORE_CALCULATED 审计日志
- [x] typecheck 0 错误；patient-risk.spec 43/43；patients.service.spec 59/59 回归

### T7: 复诊时机推荐 ✅
- [x] 9 种治疗类型默认模板（根管/洁牙/牙周/种植/充填/拔牙/冠桥/正畸/儿牙/检查/术后 14 条） → 默认建议日期与预期差 ≤ 1 天（interval 公式 bound [min,max]）
- [x] 无 PatientRiskScore → 用默认 MEDIUM=1.0；无历史依从性 → 用 0.5 中间值，推荐日不 NaN
- [x] 根管治疗实际 14 天 + HIGH 风险 × 0.7 → ≈10 天（14*0.7*1=9.8 取整 10）；adherence 差 0.2 再 ×1.64 → 16
- [x] recommend -> applyRecommendations 写入 FollowUp(PENDING) + FollowUpAssignment；一键可 Clinical 页面直接消费
- [x] 牙医可手动覆盖：PATCH /communication/follow-ups/:id 更新 planDate 不被引擎覆写（engine 同一天同模板去重）
- [x] 种植 4 次、拔牙 2 次、冠桥 2 次、术后 4 次多期模板，生成日期严格递增不冲突
- [x] batchGenerate(limit=200) 02:10 执行；失败 → BusinessAlert SCHEDULED_TASK_FAILED；关设置则跳过

### T8: 收费项目智能补全 ✅
- [x] normalizeItemKey：treatmentCatalogCode=RCT-001 → CAT:RCT-001；name 去空格转小写 → NAME:name 双重归一化
- [x] 100 条事务：60 条{A,B,C} + 30 条{A,B} + 10 条{A,C,D} → S=[A,B]，Top1=C confidence 0.667 ±0.02
- [x] 支持度计数 minSupportCount=50：C 60≥50 保留；D 10<50 过滤；minConfidence=0.8 过滤 A,B→C(0.667) 保留 A→B(0.9)
- [x] 提升度 lift：A→C = 0.7 / 0.6 = 1.167 ≥1.1 保留；A→D =1 <1.1 过滤
- [x] ignoreRecommendation A,B→C：后续 recommend 不再出现；幂等 ON CONFLICT 跳过；JSON sortKeys 保证顺序独立
- [x] 空事务/无历史：build 返回空；recommend 任意 S 空返回不崩
- [x] T<30 → buildMockDemoRules 插入 ≥20 条行业默认：洁牙+上药 / 根管+打桩+冠 / 拔牙+止血+消炎药 / 充填+酸蚀+抛光
- [x] Apriori 剪枝：maxSetSize=3（不生成 4 项集）；maxItems=8000 OOM 保护
- [x] upsertRules：ON CONFLICT(unique) UPDATE + 删除 stale 老规则；返回 added/updated/deleted 正确
- [x] 排序：antecedentSize DESC（大集合优先）> confidence DESC > lift DESC；topK=3；S=[C] consequent ∉ S（不自推）
- [x] 02:20 日调度 rebuild；Settings aiChargeAssistantEnabled=false → recommend 空；aiChargeMinConfidence/MinSupportCount/LookbackDays 覆盖
- [x] 32/32 单测 + charge CRUD 38/38 回归；typecheck 0 错；0 新依赖
- [ ] ChargeItem 表有 (chargeId, itemId) 复合索引：10 万条数据查询 < 1s

### T9: 经营异常预警 ✅
- [x] 本月营收 60 上月 100（跌 40%）→ BusinessAlert CRITICAL；营收 80/100（跌 20%）→ WARN；95/100（跌 5%）→ INFO
- [x] 本月 NO_SHOW 18% > 15% → WARN；26% ≥ 25% → CRITICAL；total=0 跳过
- [x] 新库空数据：5 类 finding 全部 skip，BusinessAlert COUNT=0，不崩溃
- [x] 阈值 Settings 可覆盖：warn=5%/critical=10% → 跌 9% 直接 WARN（默认不触发）
- [x] 医生业绩 Z=5 → CRITICAL；Z=3 → WARN；n<30 跳过
- [x] dedup：同一天 2 次 detectForClinic → COUNT=1 UPDATE 最新消息；clinicA/B 不串
- [x] v36 索引 IDX_BusinessAlert_clinic_type_occ 已建；BUSINESS_ALERT_TRIGGERED/ACKNOWLEDGED 审计；CURRENT_VERSION=36
- [x] Dashboard 接口 GET /system/business-alerts/latest?severityIn=WARN,CRITICAL / POST acknowledge 均 200 OK，Roles 限定 BOSS/ADMIN
- [ ] 仪表盘红条/黄条 Banner UI + 5 指标详情（留后续前端任务）

### T10: 库存补货建议 ✅
- [x] 90 天消耗平均 30/L=7/k=1.5：Rop 公式 ROP=ceil(avgDaily×L + SS)；avgDaily=30/30≈1 件/天，ROP≈14；库存 2 → 建议 ≥14-2=12
- [x] 库存 > ROP → 建议量不触发 ZERO_STOCK/ROP（返回 0 建议行，不写 DB）
- [x] 效期 < 30 天行：reason=EXPIRING_30D 触发（即使不缺）；效期 90 天未触发
- [x] list 一键 apply，groupBySupplier：PO number auto + 明细 SKU/建议量/单价/小计与 supplier 默认值正确
- [x] 平均采购周期 L：优先用 Settings.aiInventoryLeadTimeDaysDefault=7；后续可历史 PO 入库延迟（本任务默认 Settings）
- [x] EOQ 经典 √(2DS/H) 与 fallback minStock*2；totalAmount = suggestedQty × price；OPEN 建议重复生成时旧行 deletedAt 软删除；02:35 生成；38/38 单测

### T11: RFM + 流失预测 + 医生业绩异常 ✅
- [x] 分位点线性插值：20 个 patient 0.2/0.4/0.6/0.8 分位点对应 R=7/15/23/31（±1）；R 越小分越高；F/M 越高分越高
- [x] 张三（1天前/20次/5万）→ 「重要价值」8 段分段；李四（200天/0次/0元）→ 「流失」；REFUNDED Charge 不计 F，累计正确
- [x] 八段覆盖：重要价值/发展/保持/挽留 + 一般价值/发展/保持 + 流失；各边界（R≥4 F≥4 M≥4…）命中正确
- [x] churn 5 因素：R(0.35)+F(0.25)+M(0.20)+NO_SHOW(0.12)+FOLLOWUP_OVERDUE(0.08)；sigmod 平滑；边界 R=200 → churn≥0.9；R=1,F≥20,M≥50k → churn≤0.05
- [x] batchComputeRfm 100 patients → 8 段 breakdown 合计=100；重复 2 次 upsert 不重复
- [x] listPatients filter segment='流失' + minChurnProb=0.8 → 仅返回流失且概率 ≥0.8；排序 by churn/rfm/recency 正确
- [x] Settings aiRfmEnabled=false → compute 返回空；aiChurnEnabled=false → churnProb=null 不写
- [x] 医生 A：μ=100, σ=10，近 30 日=150 → z=5 CRITICAL；z=-1.5 NORMAL；|z|≤1.5 不写表；z=-2.2 WARN；NO_SHOW 率 0.3 vs μ=0.1 → z=4 CRITICAL
- [x] 新医生 baseline sampleSize<3 → skip 不误报；AVG_AOV_30D 暴涨 z=3.5 → CRITICAL；VISITS_30D z=-3.1 暴跌 CRITICAL
- [x] 当日去重：detectedAtDate 作唯一键；两次 batch 同 doctor+metric 不重复；resolve → resolvedAt 非空，过滤 resolved=false
- [x] Cron 02:45 RFM + 02:55 业绩异常；失败 3 次 BusinessAlert；14+10 单测、patients 59/59 回归；v43 CURRENT_VERSION=43；0 新依赖
- [ ] 打标签只增不删：人工打标签不被自动覆盖（标签前缀 auto: 区分）

---

## Phase 4: 经营运营辅助

### T12: 疗程进度看板 ✅
- [x] 逾期：completion%低 + overdueDays≥ Settings 阈值 7 → behindSchedule=1 标红；看板显示「逾期 10 天」文字
- [x] 4 状态聚合：PLANNING(DRAFT/SUBMITTED) 灰 / APPROVED 蓝 / IN_PROGRESS 绿 / COMPLETED 深绿；Dashboard 返回数量
- [x] 过滤器：doctorDashboard 支持 doctorId/fromDate/toDate；trend 支持 days=N；clinicDashboard 诊所内聚合
- [x] 空数据：plans 0 条 → 返回 0/空数组 不崩；avgCompletion = 0 且无 NaN
- [x] API 钻取：GET /plans/:id/progress 返回 items 明细（含 linkTreatment/treatmentId/price/牙齿/状态/天数滞后）
- [x] 30 天趋势补全缺日；snapshotToday UPSERT 覆盖同日；03:10 Cron；BusinessAlert 失败告警
- [x] 23/23 treatment-progress.spec；follow-ups 33/33 回归；v44 CURRENT_VERSION=44

### T13: 满意度 + NPS ✅
- [x] 20 份(12 推荐 / 5 被动 / 3 贬损) → promoters=12, detractors=3 → NPS=(12-3)/20 ×100 = 45%；nps 为百分数
- [x] NPS 边界正确：9/10 推荐；7/8 被动；0-6 贬损；=6→DETRACTOR，=7→PASSIVE，=8→PASSIVE，=9→PROMOTER
- [x] 同一 visit 2 次提交 → 抛异常（UNIQUE visitId 约束）
- [x] 5 维平均：rating=[5,4,3,2,1] avg=3.0；差评列表（nps≤6 或 rating≤2）可 list 过滤；医生排行 TOP3 样本≥5
- [x] 90 天趋势折线 + 缺日补齐；关键词 top10 自动打标签；负面 3 条件 → BusinessAlert
- [x] 03:25 Cron 快照 + 失败告警；23/23 satisfaction.spec；business-alert 27/27 回归；CURRENT_VERSION=45

### T16: 打印模板引擎 4 套标配 ✅
- [x] 变量 {{patient.name}} 嵌套 + 缺值 ''；{{{raw}}} 不转义；{{text}} HTML 转义 5 字符
- [x] if+else：truthy 非 0/null/''/false；{{#if items}}each list{{else}}空{{/if}} 组合
- [x] each：items[{n:'A'}] → {{this.n}}；空数组 → ''；深层 {{this.sub.n}}
- [x] 注释 {{!-- x --}} 剔除；8 层嵌套防护；9 层抛异常不 crash
- [x] seedDefaults 4 条（PRESCRIPTION/RECEIPT/TREATMENT_PLAN/CLINIC_REPORT）+ 幂等第 2 次不增
- [x] 处方：药品表+签名位+配伍禁忌警示（若命中）红色
- [x] 收费：明细 code/名称/数量/单价/金额 + 合计/折扣/实收/找零 + 支付方式
- [x] 治疗计划：完成度 % 进度条 + 牙位 + 已收/应收 + 预计完成日期
- [x] 诊所月报：KPI 6 项（总就诊/总收入/AOV/NPS/新患者/复诊率）+ TOP 医生 5 + TOP 库存报警 10 + 经营异常 open list + 30 天趋势
- [x] 4 套 preview sampleContext 渲染 HTML 非空；Settings aiPrintEnabled=false 禁用 4 个 render 但 preview 允许
- [x] saveTemplate 一致；setDefault 后同 clinic 同 code isDefault=1 其他=0；category 过滤 FINANCIAL 仅 RECEIPT
- [x] 22/22 print.spec；bulk-import 44/44 回归；CURRENT_VERSION=46；0 新依赖

### T14: 排班考勤请假 ✅
- [x] 同时段同员工两班（09-18 vs 10-11 重叠）→ 错误码 SCHEDULE_CONFLICT；相邻 12:00 边界不冲突
- [x] 请假申请走审批：SAVED→PENDING→APPROVED/REJECTED/CANCELLED；APPROVED 自动写 WorkSchedule LEAVE 用于考勤
- [x] 请假 REJECTED：rejectReason 必填（空抛 REJECT_REASON_REQUIRED）；申请人列表可看 rejectReason
- [x] 月视图 31 天 + hashColor(userId) 稳定色 + shift 自定义颜色；每人每天一格
- [x] 简化考勤：排班+接诊(Visit/Appointment) = PRESENT；排班但无接诊 = ABSENT；无排班/OFF = OFF；审批通过 LEAVE = LEAVE；统计 daysPresent/... 正确
- [x] 权限：DOCTOR 列表仅自己；DOCTOR 调 approve → PERMISSION_DENIED；BOSS 全量 + 审批
- [x] 软删除后同时间段再建不冲突；状态机终态不迁移；Settings aiHrEnabled=false 写接口 DISABLED
- [x] 23/23 hr.spec + print 22/22 回归；CURRENT_VERSION=47

### T15: 批量导入患者/药品/库存 ✅
- [x] 100 行患者（97 OK/3 错：2 必填缺失+1 手机格式）→ successCount=97, failedCount=3，rowErrors 带 rowIndex+field+code+message，统计正确
- [x] 错误信息不是 code，是具体可懂中文（「手机号不是 11 位」/「姓名必填且长度 1-50」…）
- [x] 50 一批小事务；失败行跳过不影响整体；批量成功后 AuditLog 写入 BULK_IMPORT_PATIENTS 带 success/failed/skipped/durationMs
- [x] phone 重复（DUPLICATE_PHONE_IN_DB / DUPLICATE_PHONE_IN_BATCH）自动跳过并报告；Drug code 重复 DB 已有→UPDATE 成功
- [x] dry-run=true：完整校验返回一致 summary/rowErrors；DB 0 写入；AuditLog 0 写入
- [x] `GET /system/bulk-import/template?type=patient|drug|inventory` 返回列定义（必填* / type / 枚举 / example / sampleRow）
- [x] Inventory strict：SKU 不存在 → DRUG_NOT_FOUND；autoCreateDrug：自动建 DrugCatalog 成功；stock 累加 2+5=7；expiryDate 格式校验
- [x] 空 rows 不崩溃；501 行超 aiBulkImportMaxRows=500 → 400「单次最大导入 500 行」；aiBulkImportEnabled=false → 403「批量导入已停用」
- [x] 模板复用一套校验/批事务机制，仅 schema（patient/drug/inventory validator）不同，不重复实现
- [x] 回归 59+8+23=90 用例全通过；v37 IDX_InventoryItem_sku_clinic 索引；CURRENT_VERSION=37；零新增第三方依赖
- [ ] 前端 UI + Excel/CSV 转换（后端只接 JSON；转换工作放前端或后续补）

### T16: 4 套打印模板
- [ ] 病历首页（A5）：患者信息 + 主诉/现病史/诊断/治疗/医生签名栏 → A4 打印预览无错位
- [ ] 处方笺（A5）：Rp 大字体 + 药品名/规格/用法用量/频次/天数，符合《处方管理办法》版式（医师签名/调配/核对/发药 4 栏）
- [ ] 80mm 热敏小票：宽度 ≤ 72mm 可用，字体不缩放，QR 码清晰（收款二维码）
- [ ] 患者标签：10 联 A4，每联姓名/性别/年龄/条码 patientNo + QR（手机扫码跳患者页 HASH）
- [ ] `@media print`：隐藏导航/侧边栏/按钮，仅打印内容区
- [ ] 空数据填充「—」不崩

---

## Phase 6: 桌面端与安全

### T17: SQLite 离线加密存储 ✅
- [x] 启用加密后：DB 落盘文件为 `.enc`，原始 sqlite3 CLI 打开报 "file is not a database"（magic DENTAL_DB_ENC 非 SQLite 头）
- [x] 正确密钥启动：database.ts / db.service.ts 启动流程零改动（透明 wrapper 自动解密到 tmp）；db.service 37/37 回归全过
- [x] 错误密钥启动：authTag 校验失败 DB_ENC_AUTH_FAILED，加密文件未被改写，重试正确密钥可正常进入（单元测验证）
- [x] 首次加密迁移：明文不存在→boot 自动新建空 plain DB→首次 persist 原子写 enc.tmp→rename；失败不破坏
- [x] 备份文件：加密文件可直接作为备份拷贝；恢复只需同路径 + 密码解密
- [x] 密钥派生：PBKDF2(sha-512, 600,000 轮, 16B salt)；密码空且 enabled=true 抛具体可操作异常「DB_ENCRYPTION_PASSWORD 未设置」
- [x] 加密开关 OFF 时（aiDbEncryptionEnabled=false）：完全不启用 wrapper，老路径零影响；可手动 decryptFile 迁移回明文（提供工具函数）
- [x] 额外：心跳 10 min 持久化、shutdown 3 次重试、rotatePassword、4 种审计；20/20 单测；v38 空迁移占位

### T18: Electron 托盘 + 最小化后台
- [ ] Win 10/11 任务栏通知区可见图标，悬停显示系统名
- [ ] 托盘菜单：「打开主窗口」「退出应用」两项全部生效
- [ ] 默认设置 closeToTray=true → 点 X → 主窗口隐藏 + 进程存活 + 托盘可见
- [ ] 设置改为「关闭即退出」→ 点 X → 进程退出 + 托盘销毁
- [ ] 多次快速开关窗口不崩溃不泄漏（BrowserWindow 单例不重复 new）
- [ ] 托盘图标使用 16x16/32x32 多分辨率，Windows DPI 缩放不糊

---

## Phase 7: 正畸头影测量

### T19: 头影测量全流程 ✅
- [x] 50+ 项指标公式（Steiner 7 / Downs 8 / Tweed 3 / McNamara 5 + 角度 20+ / 线距 15+ / 比例 5）纯 TS 数学；3 份教材坐标容差 0.5°/0.3mm
- [x] 30 个标志点缺一 → 报错明确中文「缺少 N 鼻根点」；validateLandmarks 返回 missing label 列表
- [x] 4 方法（Tweed/McNamara/Steiner/Downs）切换：computeAnalysis(method) 返回对应子集；全方法合并 ≥50 项去重
- [x] 正常值对比：23 项核心指标硬编码范围 + DB CephalometricNormValue 覆写；classifyDirection → UP(红)/NORMAL(绿)/DOWN(蓝)
- [x] 打印报告：第 5 套模板 CEPHALOMETRIC_REPORT（A4）含 SVG 点连线叠绘 + 指标表 4 方法分 tab + 医生签名栏
- [x] 历史对比：compareRecords(id1,id2) → 每指标 delta + arrow ↗/↘/→
- [x] Settings aiCephalometricEnabled / aiCephalometricDefaultMethod / aiCephalometricScaleFactor
- [x] 修复 settings.loadFromDb clinic 覆盖排序 + system.tables.ts PrintTemplate schema
- [x] 56/56 cephalometric.spec + print 22/22 + hr 23/23 回归；CURRENT_VERSION=48；0 新依赖

---

---

## Phase 8: 前端 UI 对接（Phase1-7 后端就绪后补充）

### FE-T9: 经营异常预警工作台 ✅
- [x] Dashboard 顶部 AlertBanner：open>0 时显示「当前 N 条未解决 / M 条严重」5 秒轮播；严重度配色 CRITICAL 红/WARN 橙/INFO 蓝；【查看详情】跳转
- [x] BusinessAlertPage 4 KPI（未解决/严重错误/待确认/已解决本月）→ 点击任一自动同步 Filter
- [x] Filter Bar：搜索框 + 状态多选 + 严重度下拉 + 类型 12 枚举 + 时间范围 7/30/90 天 + 刷新
- [x] 主表格：严重度 Badge / 类型图标 / 摘要 Tooltip / 状态点 / 关联实体超链接 / 创建时间相对时间 / 操作列
- [x] 多选 + 批量解决 Dialog；行级 PATCH acknowledge / PATCH resolve
- [x] AlertDetailDialog：顶部状态色 + message + metadata key/value 表 + Timeline + 备注区 Add Note
- [x] 13/13 business-alerts.test；DashboardPage 4/4 回归；typecheck 0 错

### FE-T12: 疗程/治疗进度看板 ✅
- [x] 筛选栏：状态/医生/滞后风险 5 级多选/时间范围/搜索/刷新/批量打印选中
- [x] 6 KPI：进行中/已完成/平均完成%环形/平均滞后/严重预警数/今日到期
- [x] 总览 Tab：风险分布 5 色环饼 / 30 天滞后趋势堆叠 / 计划状态饼
- [x] 进度看板 Tab：患者(首字头像+性别年龄)、计划名、主治、创建→目标倒计时、进度条 4 色 ≥80 绿/50-79 黄/30-49 橙/<30 红
- [x] 财务：应收 20000 / 已收 15000 → 未收 ¥5000 红色差额；已收=应收则 √
- [x] 滞后预警 Badge 5 色：NONE灰/LOW蓝/MEDIUM黄/HIGH橙/CRITICAL红
- [x] 行展开：治疗项(牙位/名称/价格/状态 TODO IN-PROGRESS COMPLETED CANCELLED + 日期)
- [x] 今日到期 Tab 子集高亮倒计时；详情 Dialog：items + snapshots 折线 + timeline 4 种事件
- [x] 12/12 treatment-progress.test；TreatmentPlanPage 1/1 回归；0 新依赖

### FE-T13: 患者满意度 + NPS 统计 ✅
- [x] KPI 6 卡片：总调查 / Promoter / Passive / Detractor / NPS 环形色阶 ≥60 绿 ≥30 黄 <30 红；5 维均分条
- [x] 趋势 Tab：30 日 NPS 折线+调查数柱双轴；NPS<30 红 / 30-60 黄 / ≥60 绿
- [x] 医生排名 Tab：柱图按 NPS 排序；样本 <30 灰字标注样本不足；≥30 显示色柱
- [x] 差评列表：NPS0-6 或 rating≤2；列=时间/患者/医生/NPS/星级平均分/负面标签/评论摘要/跟进状态；每行【跟进】
- [x] 关键词 Tab：Top20 词频条形图(正面绿/负面红/中性灰)；可 Filter NEGATIVE/POSITIVE/NEUTRAL
- [x] 右侧栏：最近 20 条评价 5s 自动轮播 + 发起评价 CTA
- [x] SurveyDialog：visit 下拉 + NPS 0-10 彩色滑块 + 5 维 5 星 + Comment + Tags(正负 10 预设 + 自定义) + 来源 RadioGroup(QR_CODE/SMS/LINK/MANUAL)
- [x] AcknowledgeDialog：显示评价详情 + 跟进备注 Textarea + 标记已跟进 PATCH
- [x] 筛选栏：时间 7/30/90/自定义 + 医生筛选 + NPS分类 + 关键词搜索 + 刷新
- [x] 22/22 satisfaction.test；FollowUpsPage 3/3 回归

### FE-T14: 排班考勤请假 HR ✅
- [x] 3 Tab：【排班日历】【考勤统计】【请假管理】；路径 /hr；roles=[BOSS]
- [x] 月视图 6×7 格；格顶日期；格内按人横排稳定色卡条（早/晚/全/假/休/定短标签+人缩写）；点击空白/已有 → ScheduleDialog
- [x] 格底考勤状态：PRESENT 绿勾 ✓ / ABSENT 红 × / LEAVE 蓝假 / OFF 灰休
- [x] ScheduleDialog：人员 select / shiftType(6 RadioGroup) / 自建 DateRangePicker(日期+时分) / Note / repeatRule 下拉 / 颜色预设+自定义；SCHEDULE_CONFLICT → 顶部红色 Alert
- [x] 考勤 KPI 4 张：出勤(绿)/缺勤(红)/请假(蓝)/休息(灰) + 出勤率 % = 出勤/(出勤+缺勤)；月份筛选 + 人员
- [x] 考勤列表：员工 × 应出勤/实出勤/缺勤/请假/休息 + 出勤率条形 + 迟到；行展开每日详情
- [x] 请假管理：筛选状态/类型/申请人/时间/搜索；KPI=待审批 N / 本月 M / 剩余年假 Y 天
- [x] 请假列：申请人/类型色标/时段+天数/原因摘要/状态徽章(草稿灰/审批中橙/通过绿/拒绝红/撤销灰)/审批人/审批备注/操作
- [x] LeaveDialog：申请人(BOSS 可选所有员工) / 类型 8 RadioGroup + 年假余额 / 起止+全天 / 天数自动向上取整 / 原因必填；【保存草稿】【提交审批】双按钮
- [x] BOSS 审批：双按钮【通过】/【拒绝 rejectReason 必填空填则前端 Alert】；PENDING 状态行高亮
- [x] 12/12 hr.test；StaffPage 4/4 回归；typecheck 0 错

### FE-T15: 批量导入患者/药品/库存 UI ✅
- [x] parseCSV 纯手写：引号转义、\r\n 与 \n 兼容、空文件安全；toObjects 列名-值映射；后缀自动检测分隔符
- [x] BulkImportPage 3 Tab：患者/药品目录/库存；左侧 4 步指引：下载模板→上传→预览→确认导入
- [x] 下载模板：调 API 取列定义 → 生成 UTF-8 BOM CSV(列名+1行示例) → 触发浏览器下载
- [x] DropZone：拖放+点击上传；.csv/.tsv/.txt 接受；.xlsx Alert「请另存为 CSV UTF-8」
- [x] 预览：前 10 行；表头高亮；必填列 *；文件大小/行数显示
- [x] 选项：dryRun 默认 true 绿色安全；strict 默认 true(SKU 严格)；autoCreateDrug 默认 false 琥珀谨慎(库存 Tab 显示)
- [x] 校验结果：成功/失败/跳过 3 KPI 卡 + 失败行表格(行号+字段+错误码+中文消息)红底高亮
- [x] 正式导入按钮：仅 failedCount=0 显示；dryRun=false + 成功 Toast「导入成功 N 条，耗时 Xms」
- [x] BulkImportDialog 组件导出（可嵌入 SettingsPage 按钮弹出）
- [x] 19/19 bulk-import.test（含 F15.1-10 全覆盖 + TSV / toObjects / 换行处理 + 列填充）

### FE-T16: 打印预览 5 套模板 + @media print CSS ✅
- [x] PrintPreviewPage URL query：?type=prescription|receipt|treatment|clinicReport|cephalometric|template&id=&code=&month=
- [x] 左侧 6 Tab：处方笺/收费凭证/治疗计划/诊所月报/头影报告/自定义模板；右侧工具条+iframe srcdoc 注入 HTML + sandbox
- [x] 工具条：刷新/下载 HTML/复制 HTML/打印 4 按钮；纸张 A4(210×297mm)/A5(148×210)/RECEIPT(80mm) 切换→容器 class 对应
- [x] 自定义模板：code 下拉 + SampleContext 4 套示例切换 + textarea JSON 编辑器 + 格式化按钮 + 手动【预览】
- [x] 空状态：5 个大卡片入口，提示「无真实 ID 可用，点击进入模板预览」
- [x] 全局 CSS：@media screen（no-print 显示/print-only 隐藏）；@media print 隐藏导航/侧边栏/顶栏/对话框/按钮/toast；显示 print-only；page-break-after 分页；去阴影；3 纸张 @utility 类
- [x] 路由注册 /print-preview hideInNav=true；PrintPreviewButton 组件 + buildPrintUrl()
- [x] 8/8 print-preview.test；SettingsPage 3/3 回归；0 新依赖

### FE-T19: 头影测量 画布 + 指标分析页 ✅
- [x] CephalometricCanvas(SVG)：30 点拖放/放置/选择/Pan；工具条切换模式；Undo/Redo(显示步数)；保存/分析/下载 SVG/上传影像占位/重置缩放/打印
- [x] 坐标系：左下原点 (0,0)，x 右 y 上；SVG y 轴翻转 toSvg/fromSvg；10mm 刻度网格背景 + 纵横标尺
- [x] 键盘微调：↑↓←→ 0.5mm；Shift+方向 2mm；Ctrl/Cmd+Z Undo；Ctrl/Cmd+Shift+Z Redo；Delete 删除选中点；1-9 数字键快速切 place 模式
- [x] Undo/Redo 栈：50 步最大深度（第 51 步丢弃最旧）；拖动 debounce 300ms 批量入栈（像素密度级不入栈）
- [x] 右侧点清单面板 30 行：颜色标 + code + 中文 label + x,y 输入框 / 未放置 + 锁定 Checkbox + 快捷定位图标
- [x] 参考线图层 7 条可独立开关：SN(蓝)/NA(蓝)/NB(蓝)/ANS-PNS 腭平面(紫)/Go-Me(橙)/Go-Gn(绿)/Po-O FH(灰) — 虚线
- [x] 必填点缺失校验条：红色提示「缺少 N(鼻根点) 等 3 个点」；点击【去放置】自动跳转到第一个缺位点进入 place 模式
- [x] MetricsTable：4 方法折叠分组表头；列=指标名 / 数值+单位 / 正常范围 [min,max] / 方向 Badge UP红↑ NORMAL绿— DOWN蓝↓ / 点击行 tooltip 公式说明
- [x] CompareView：两个历史分析记录下拉；对比表=指标 / 记录 1 值 / 记录 2 值 / delta / 箭头 ↗↘→；Toggle「只看差异项」
- [x] CephalometricPage 三栏：左侧患者选择 + 历史记录列表 + 新建；中间 Canvas；右侧分析面板 = All/Steiner/Downs/Tweed/McNamara Tab + 【执行分析】+指标表+对比 Tab + 打印跳转
- [x] 14/14 cephalometric-canvas.test + 12/12 cephalometric-analysis.test；ImagingPage 1/1 回归；typecheck 0 错
- [ ] 不引入任何医疗级深度学习模型：所有算法为规则/统计/几何/公式，可在代码中 100% 复现
- [ ] 不破坏现有 142 套 3541 条 API 测试 + 36 文件 97 条 Web 测试：全部 0 失败
- [ ] TypeScript 类型检查零错误
- [ ] ESLint error=0（warning 总量不增超过 5 条）
- [ ] 所有新增接口均走 RolesGuard/@Roles：public 接口有 @Public() 并经 audit-log 审计
- [ ] 所有新 SQL 参数化：搜索代码 `\$\{` 无直接字符串拼接（遵循 `.qoder/rules/sql-parameterization.md`）
- [ ] 不引入需要 SLA 的第三方外部 API 服务（纯 npm 包依赖除外）
- [ ] 交付可验证文件路径：
  - [spec.md](spec.md)
  - [tasks.md](tasks.md)
  - [checklist.md](checklist.md)

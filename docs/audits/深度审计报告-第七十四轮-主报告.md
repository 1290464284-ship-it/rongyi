# 第七十四轮评分覆盖率口径修复主报告

## 结论

重新评分时发现 `quality-score.mjs` 的 coverage 指标与 Vitest 报告不一致：branches 被高估为 98.37%（实际 86.68%），lines 恒为 100%（实际 97.15%）。

## 完成内容

- 修复 branches 统计：原来按分支位置只要任一路径执行就算命中，改为按分支路径逐条统计，与 Vitest 一致。
- 修复 lines 统计：v8 `coverage-final.json` 没有行级字段，原实现恒输出 1；改为从 `statementMap` 推导唯一行覆盖。
- `coverageStats` 抽为 `quality-metrics.mjs` 纯函数，并补 2 个 spec 覆盖分支路径与同多行语句去重。

## 最新评分

- Coverage：statements 95.19 / branches 86.68 / functions 98.98 / lines 97.15。
- Mutation：90.24。
- OpenAPI：route path coverage 100%。
- Flaky history：4 runs / 1 failure（历史数据）。

## 验证

- typecheck、lint、knip、unit tests 通过：239 files / 2423 tests。
- `test:coverage` 阈值通过；`test:quality-score` 输出与 Vitest 一致。

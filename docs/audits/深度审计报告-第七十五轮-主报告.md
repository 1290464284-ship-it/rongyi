# 第七十五轮 P0-P2 收口与完整交付门禁主报告

日期：2026-08-13

## 结论

本轮完成质量冻结解冻清单中的剩余 P0/P1/P2 项：

- P0-2：Web branch coverage 91.88% -> 93.01%（5432/5840）。
- P0-3：mutation 90.24 -> 96.54，超过 high 93。
- P0-4：D-006 正式归档为内部兼容输入/产物。
- P1-5/P1-6：OpenAPI generated path 使用 `{param}` 模板并声明参数；live 覆盖全部 GET 路由与 110 个资源 handler；资源列表列与声明 schema 一致性校验。
- P1-7/P1-8：benchmark 指标写入性能历史；新增聚合快照冷/热态基准并接入 `test:performance-history`。
- P1-9：CSV 真流式收益评估归档，更新 D-003。
- P1-10：quality-score 数值评分、历史记录、committed baseline、CI artifact 上传；连续两轮下降或低于基线即失败。
- P2-11/P2-12：本轮报告与归档索引更新；完整交付门禁复跑。

## 最新指标

- 服务端 coverage：96.29 / 90.46 / 99.46 / 97.83。
- Web coverage：96.82 / 93.03 / 98.57 / 98.53。
- 测试：258 文件 / 2611 用例。
- Mutation：96.54（break 88 / low 90 / high 93）。
- OpenAPI：route path coverage 100%。
- 自动质量评分：95.90。

## 交付门禁

| 门禁 | 结果 |
|---|---|
| build | PASS |
| electron:compile | PASS |
| verify:package | PASS（按用户排除签名闭环，跳过 publisher/cert 校验） |
| smoke:api | PASS |
| smoke:ui | PASS |
| test:load | PASS（avg 14.1ms / p95 16.5ms） |
| test:large-db-concurrency | PASS（100001 患者/收费/预约等并发读写） |

## 过程中修复的真实问题

- `UserRole` 无 id 表通过通用资源列表返回 500：仓库支持无 id 列排序/游标。
- `ResourcePage` 批量删除失败后对话框开关方向反转。
- dev CORS 只放行 5173/5180，随机端口的 Vite 被拒；改为开发模式放行任意回环端口。
- Vite proxy 用 `localhost` 而 API 只监听 `127.0.0.1`；改为回环地址直连。

## 验证

typecheck、lint、knip、全量单测全绿；benchmark 与 mutation 阈值通过。

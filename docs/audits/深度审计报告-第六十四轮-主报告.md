# 第六十四轮 CI 门禁强化主报告

## 结论

本轮把完整 shuffle 门禁从独立 scheduled workflow 拉入 `v2-ci` 的 PR 必跑路径。

## 完成内容

- `v2-ci.yml` 在 Unit tests 后新增 `Full shuffle tests`，每次 PR 执行一次 `--sequence.shuffle`。
- 顺序依赖回归将在合入前被直接拦截。

## 验证

- 工作流语法已更新。
- typecheck、unit tests 通过。

# 第七十二轮 errors 契约断言提分主报告

## 结论

本轮强化 `errors.ts` 的契约断言，mutation score 从 84.77 提升到 87.44。

## 完成内容

- `errors.spec.ts` 对 `AppError`/`ValidationError`/`NotFoundError`/`ConflictError`/`UnauthorizedError` 补齐 name/code/message/status 全量断言。
- `asAppError` 的 parse-failed、too-large、CORS、INTERNAL_ERROR、AppError 透传分支补齐 message 断言。
- `errors.ts` mutation score：63.33 -> 85.00。
- `pagination.ts` 提升至 95.92，`validation.ts` 提升至 89.04。

## 验证

- typecheck、lint、knip、unit tests 通过：239 files / 2420 tests。
- 完整 `--sequence.shuffle` 通过：239 files / 2420 tests。
- `test:mutation` 通过：87.44 >= break 80。
- `test:quality-score` 输出 mutation score 0.8744、route path coverage 1。

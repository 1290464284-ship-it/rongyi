# 全局优化审查 — 执行台账（2026-08-16）

> 来源：五份专项报告（server / web / electron / 工程 / 测试，见 `docs/audits/全局优化审查-*.md`）。
> 本文件是执行记录与待决事项的唯一台账：✅ = 已修复并提交；⏳ = 待用户决策/后续轮次。
> 提交记录：`52a0cb11`（server/electron）、`6d043005`（脚本/门禁/文档）、`648725da`（web 批量）、`ffaa4f75`（v8 清理 + 质量分基线）、`c4798e44`（打包证书 + 台账）、`6defaf6a`（台账回填）、`ccc8bfa5`（S-1/S-3/W-4）、`0e0b6b8b`（T-1 覆盖单进程收集根治）、`3dd10c16`/`f93e0bd3`/`6c4576dc`（W-11 拆分三批）、`23cf6bfc`（2.2.1 版本）、`d963ea85`（verify:package E-5 兼容）。

## 1. 已执行（✅）

### 二轮补充批（ccc8bfa5）
- **S-1 身份证列表脱敏**：通用列表响应按资源掩码（`router.ts` LIST_MASK_FIELDS，身份证保留尾号，与 CSV 导出共用 `maskIdCardValue`）；完整值收敛到详情接口；`CrudPage` 新增 `onEditLoad` 异步补充钩子（epoch ref 防过期响应串扰、加载期间提交禁用），`PatientsPage` 编辑时从详情拉取 idCard 回填，提交前对掩码值兜底拦截（防掩码写回库）。search 端点本就不含 idCard，无需处理。
- **S-3 计划总价一致性护栏**：`saveTreatmentPlan` 未划价时校验 `totalFee === Σ(明细有效小计) × (1-计划折扣率)`（与 `TreatmentPlanBillingService.planTotal` 同口径，明细级折扣按存量行计）；不一致时抛 ConflictError，客户端手动改过总价（`totalFeeConfirmed`）才放行。前端 PlanFormFields 总价输入置位确认标记、状态改 select。
- **W-4 枚举自由文本 → select**：PlanFormFields 状态、CephalometricFormFields 状态接入 `CLINICAL_STATUS_LABELS` select；`templateId` 与 `imaging.type` 经核为自由文本字段（无枚举源），保留文本输入并注释说明。

### Server / Domain
- 1.1 治疗计划 edit-save 划价锁定缺口：`saveTreatmentPlan` 主表 UPDATE 并入 billed 守卫（与 setPlanDiscount/通用 PATCH 三处对齐）。
- 1.2 会员折扣 ROUND 取整超收：`total = Math.min(baseTotal, roundTotal(...))`，禁止 total > baseTotal 与负 finalDiscount。
- 2.1 提成计算 `IN` 超参上限：抽 `commission-items.ts` 按 ≤1000/批查询（模块行数回落 454→436）。
- 2.5 微信提醒生成 N+1：`loadExistingKeys` 批内一次 IN 载入已存在键集合，替代逐候选 exists（唯一索引 + INSERT OR IGNORE 兜底并发）。
- 3.1 时区偏移双源：`read-routes.ts by-date` 改用 `clinicDayStartUtc/clinicDayEndUtc`（顺带把非法日期从 500 变为 400）。
- 3.2 临床工作流仓储表名白名单断言（Registration/Visit/FirstExam/Treatment/MedicalRecord）。
- 3.5 会员卡多卡隐式选择：`findByPatient/findByPatientForRefund` 显式 `ORDER BY createdAt ASC, rowid ASC`（同时间戳按插入顺序，兼容既有行为）。
- 3.6 `main.ts` 双 import 同行拆分。
- 授权/过滤分支补测并移除 v8 排除：`router.ts:50-55`（新增 UserPermission 撤销 supertest 用例）、`repository.ts:175-178`（新增对象/数组过滤器用例）。

### Electron
- H1 API 重启端口复用（`state.apiPort` 未被占用即复用，消除 CSP 漂移）；H2 重启标记顺序；H3 启动清除 `.supervisor-stop`；H4 safeStorage fail-closed（会话内密钥 + 拒绝明文备份）。
- M1 头 CSP 与 meta CSP 双轨注释澄清（header 仅 dev 生效）；M2 微信 AppId 单一通道（env 白名单移除 V2_WECHAT_APP_ID，只走 secret file）+ 注释修正 + spec 断言；M3 TOCTOU 注释修正；M6 删除死前置重置；M7 更新退避定时器句柄跟踪与清理；M8 错误消息引用常量。
- L3 解密密钥复用 `isUsablePlainSecret` 校验；L4 去重注释；L5 窗口状态写盘收敛（托盘路径保存、closed 兜底）；L6 error.html 增加 meta CSP。
- E-4 api-process.cjs 魔法数字命名化：健康检查超时 500ms / 重试间隔 400ms / 进程终止宽限 5s·1.5s·200ms 提取命名常量。

### Web
- P0-1 收费列表分页：ChargesPage 接入 `PagePager`（此前只显示前 50 条）；删除末页回退分支由死代码变为可达并移除 v8 排除。
- P0-2 扫码定位：先服务端精确过滤 `code`/`barcode`，未命中再退化全文搜索（消除前 20 条截断误报）。
- P0-3 批次全量拉取：`enabled: Boolean(itemId)`（未选项目不再全量拉批次）。
- P1 分页补齐：MedicalRecordsPage / TreatmentPlansPage / PrescriptionsPage / VisitsPage / TreatmentsPage / FirstExamsPage / CephalometricPage / MemberCardsPage / ImagingPage / ProcessingOrdersPage / PurchaseOrdersPage 全部接入 `paged` 分页器。
- P1 医生下拉收敛：`hooks/use-doctors.ts`（`['doctors']` + `staleTime: Infinity`）+ `components/DoctorSelect.tsx`，迁移 12 处调用点（AnalyticsDashboardPage 经核实请求 `/satisfaction/doctor-rankings` 非 `/doctors`，保留原逻辑）；新增 use-doctors.spec 6 用例。
- P1 followUpStatus 双文案收敛：删除 treatment-plans 私有 `FOLLOW_UP_LABELS`，统一用 `lib/labels.FOLLOW_UP_STATUS_LABELS`（服务端口径：待跟进/需横向转诊/横向已转/已流失）。
- P1 写操作 ref 级 in-flight 守卫：SendWechatDialog / RecordDialog / CreateFollowUpDialog / TrackingDialog / ReportDialog 五个缺守卫的写入口补齐（连点不再重复非幂等 POST）。
- 收费页 pay-methods 加载失败提示 + 重试（不再静默回退内置方式）。
- ClinicalWorkflowPage stale 按资源维度计算（翻一资源分页不再连坐其它区块）。
- 状态列中文化：medical-records / cephalometric / purchase-orders / FinanceWorkflowPage（欠费）/ TreatmentPlansPage 状态列接入 label 映射。
- 关联就诊下拉显示可读 label（时间 + 短 ID）替代裸 UUID。
- 盘点创建失败不再清空输入（仅成功后清空）；报表截断提示改显示实际条数。
- 渲染期写 ref 反模式收敛：MedicalRecordsPage staleRef / PrescriptionsPage updateFormRef 迁到 effect 同步组件（与既有 M9 约定一致）。
- 创建预约成功后表单重置；UsersPage `/auth/me` 缓存键与 Layout 统一（`['me']` 共享缓存）。
- 移除 8 处「无分页 → stale 恒 false」类过时 v8 排除（分页接入后守卫可达）。
- W-7 手写 busy 状态机迁移 useAsyncAction：InventoryPage / FinanceWorkflowPage / MemberCardPlanDialog / DispenseNarcoticPanel（保留 ref 级同步防重；ChargesPage 由另一任务处理）。
- W-3 日期/金额格式化统一：`imaging/format.ts` 的 formatDateTime/toLocalDatetime 改为委托 `lib/format`（无效日期行为升级为原样返回）；VisitsPage / AppointmentBoardPage / appointments 列的手写 toLocaleString 收敛 `formatDateTime`；TreatmentPlansPage 回访列日期只显日期部分、总价回填用 `centsToYuanString`；InventoryWorkflowPage 盘点开始时间收敛 `formatDateTime`。VisitsPage/TreatmentsPage 的 `slice(0,10)` 是 date 输入控件值（非展示列），保留。
- W-5 字典查询 staleTime：ChargesPage charge-trees / pay-methods/tree 加 `staleTime: 5min`（/doctors 已由 useDoctors 的 Infinity 覆盖）。
- W-10 工具函数下沉：新建 `lib/csv.ts`（csvCell + downloadTextFile，含公式注入防护与 BOM 下载），analytics-utils 改 re-export（`today` → `todayLocalDate`）；ResourcePage 及其 spec 改从 lib 导入（消除共享组件依赖页面模块的分层倒置）。
- W-12 ID 截断宽度统一：InventoryWorkflowPage 单号/库存项目列 14/12 → 8。
- W-9 评估结论（不迁移）：各模块私有字典（PRESCRIPTION_STATUS / REVIEW_STATUS / EDIT_STATUS / CATEGORY_TYPE / PHASE / REPORT_TYPE / DICT_TYPE / PLAN_DISCOUNT / TYPE 等）均为单域专用、键集与 `lib/labels` 无重复；真正的重复字典（followUpStatus）已收敛。为免单域常量搬迁造成无价值 churn，维持模块内定义。

### 工程链
- delivery-drill 崩溃 stderr 保留（不再清空）；`pickFreePort` 统一空闲端口探测（delivery-drill / delivery-smoke-runner / crash / disaster / legacy-dirty / permission / simulated-data / http-fuzz / soak / environment / multi-instance 11 个脚本）；delivery-smoke-runner 动态端口 + 子进程退出后再删临时目录。
- 未消费的 stderr/stdout 管道改 `inherit`（7 个脚本，消除背压挂起风险）。
- 模拟库管理员口令单一来源：`lib/sim-admin.mjs`（10 个脚本统一引用，契约指向 simulate-clinic-data.ts）。
- sbom.mjs 显式 import randomUUID；flaky-report 全历史失败数 + 近 14 天窗口；audit.mjs 失败保留 stderr；clean-generated 补 coverage-web/release-v2-internal；v8-ignore-report 增加单文件 ratchet；quality-score 暴露 mutation ignored 计数。
- `.npmrc` 死配置删除（pnpm 11 只认 pnpm-workspace.yaml allowBuilds）；.gitignore 补 `release-v2-internal/` 与根 `logs/`。
- 门禁收紧：server branches 85→95 / statements 95→98 / functions 97→98 / lines 95→98；web 四项 86/75/84/90 → 95/95/95/95；CI FLAKY_RUNS 1→2。
- 文档漂移：docs/audits/README（指向第 77 轮 + 专项报告）、refactor-v2-architecture 目录图、release-modes 内部产物路径/tag 说明；coverage-exclusions.md 台账同步 294→276 与分页接入后的条目更新。
- T-6 抽 `scripts/lib/drill-runtime.mjs`：`createDrill(...)` 工厂 + 独立 `assert`，统一 baseEnv/spawnApi/startApi/stopApi/request/waitForApi/assert 六类公共基础设施；迁移 9 个脚本，multi-instance 保留并注释说明。

  - **T-6 细节**：新建 `apps/v2/scripts/lib/drill-runtime.mjs`，导出 `createDrill({ appRoot, serverScript, legacyDb, legacySchemaDir, dataDir, backupDir, logDir, port, jwtSecret, backupKey, adminPassword, dbPath?, envExtra?, stdio?, captureStderr?, waitTimeoutMs?, readyLabel? })` → `{ baseEnv, spawnApi, startApi, stopApi, request, waitForApi, assert, base, apiProcess }`，另导出 `assert(condition, message)`。统一口径：`stopApi` 以 exit 事件 + 5s SIGKILL 兜底（改用 `exitCode/signalCode` 判断，修掉 delivery/disaster 用 `child.killed` 导致 SIGKILL 兜底失效的隐患）；`waitForApi` 默认 30s（soak 传 60s）；`request` 保持「`response.ok && body.success` 才返回 `body.data`，否则抛 `${method} ${pathname}: ${status} ${JSON.stringify(body)}`」的错误消息格式不变。
  - **迁移清单**：delivery-drill（保留 restore/verify 流程 + `captureStderr` 崩溃 stderr 保留）、disaster-drill、crash-drill（保留 kill -9/taskkill 的 `forceKillApi` 场景）、legacy-dirty-drill（保留 `waitForApiOrExit` 与脏库构造）、http-fuzz-smoke（保留 `rawRequest`/`expectClientError`/`expectNoServerError`）、permission-smoke（保留 `V2_REQUIRE_PERMISSION_SMOKE` 与 `rawRequest` 断言）、soak-smoke（保留计时 request 与 p95/完整性校验）、environment-drill（多进程 children + 端口复用，仅复用 `assert`，参数化的 waitForApi/request/startApi 保留并注释说明）、wechat-gateway-smoke（动态网关 env 走 `startApi(overrides)`）。multi-instance-smoke 为双实例按 port 参数化 + 每进程独立 stderr 缓冲，与单实例工厂签名不匹配，保留本地进程管理并注释说明。
  - **验证**：`node --check` 11 个文件全 ✅；实跑 http-fuzz ✅、permission ✅（`apps/v2/data/simulated-clinic` 已存在，未跳过）、delivery ✅（`V2_ADMIN_PASSWORD=v2-sim-admin-password`）、disaster ✅、crash ✅、legacy-dirty ✅、soak ✅（`V2_SOAK_SECONDS=10`，从 `apps/v2` 目录运行）、environment ✅、wechat-gateway ✅。
  - **备注**：soak-smoke 的 `require(path.resolve('node_modules/better-sqlite3'))` 依赖 cwd，从仓库根直跑 `node apps/v2/scripts/soak-smoke.mjs` 会 MODULE_NOT_FOUND（better-sqlite3 位于 apps/v2/node_modules），需从 apps/v2 目录或经 `pnpm --filter @dental/v2 soak:smoke` 运行——属既有行为，本轮未改动。

### 测试
- auth.service 5.1s 真实 sleep → fake timers（`advanceTimersByTimeAsync`），套件提速约 4s。
- equivalent-mutants 白名单行号对齐（wechat-reminder 270→281，13/13 精确匹配）。
- 质量分基线刷新：100 → 96.95（下降全部来自 flaky 窗口收紧后计入的 08-12 真实抖动记录，覆盖率/变异/路由分项未退）。
- T-2 真实 sleep → fake timers：logger.spec 700ms / use-crud-resource 400ms×4 / SystemOperationsPage 350ms×5 / database.spec 退避迁移 `vi.useFakeTimers()` + `advanceTimersByTime(Async)`。

## 2. 待用户决策 / 后续轮次（⏳）

### Server
| 项 | 内容 |
|---|---|
| （无待决项；S-2 已随 `1735ecca` 完成见 §4；S-4/S-5 经核维持见 §8） |

### Electron
| 项 | 内容 |
|---|---|
| （无待决项；E-2/E-5 已在本轮完成，E-1/E-3/E-4 早前完成） |

### Web
| 项 | 内容 |
|---|---|
| （无待决项；W-11 12 个大文件已全部拆分完成，见 §9；W-14 经核维持见 §8） |

### 测试/门禁
| 项 | 内容 |
|---|---|
| （无待决项；T-1 已在 2026-08-17 完成，见 §7） |

## 3. 门禁状态（2026-08-17 快照，v2.2.1）

- typecheck ✅ / lint ✅ / knip ✅ / 全量测试 **3297** ✅（含 flaky-detect 2 次 shuffle 复跑）
- test:coverage ✅（server 单进程收集：workflow.ts 136/136；web 单进程：DispenseEditDialog 63/63、PatientTimelinePage 106/106）
- v8-ignore ratchet：**259** 处 = 基线 259 ✅（含单文件）
- 质量分 **98.36**（基线已更新）
- build ✅ / electron:compile ✅ / verify:package ✅ / verify:desktop-sync **47/47 双向** ✅
- **发布 v2.2.1** ✅：https://github.com/1290464284-ship-it/rongyi/releases/tag/v2.2.1
- smoke:api / smoke:ui / test:load 于发布后补跑（见 §11）。

## 4. 结构性批次与发布（2026-08-16 晚）

- **S-2 专用列表 keyset 化**（提交 `1735ecca`）：新建 `infrastructure/keyset.ts`（keysetCondition/keysetOrder/nextCursorFrom，支持单列与多列游标）；发药/退款/盘点/采购审核/麻醉登记/随访提醒/业务告警/通知 8 处专用列表 + 路由统一迁移：两模式均按 keyset 排序（id 决胜键）、恒取 pageSize+1 行、恒回传 nextCursor；v160 迁移补 4 张表 `(clinicId, createdAt)` 复合索引（Refund/Stocktake/PurchaseOrder/FollowUp）。hr 考勤（workDate/checkIn 业务排序）登记为例外。keyset.spec 7 用例 + dispense/stocktake/alerts/follow-up 4 个游标对账用例。
- **T-6 drill 运行库抽取**（`1735ecca`）：新建 `scripts/lib/drill-runtime.mjs`（createDrill 工厂：baseEnv/spawnApi/startApi/stopApi/request/waitForApi/assert），迁移 9 个脚本（delivery/disaster/crash/legacy-dirty/http-fuzz/permission/soak/environment/wechat-gateway），multi-instance 保留本地实现并注释；9 个脚本实跑全部通过。
- **W-6 对账去重**（`1735ecca`）：新建 `lib/reconcile.ts` 泛型 `reconcileItems`（purchase/processing 两处对账收敛，差异经 isValid/toPatch/toPost 保留）+ `hooks/use-remote-search.ts`（RelationSelect 与 SearchableSelect 共用，差异经 pageSize/keepPreviousData/mergeMode 保留）。
- **W-11 大文件拆分**（`5f3906eb`）：UsersPage 432→277、ImagingPage 405→122、DispenseNarcoticPanel 355→198、PatientsPage 326→117，新增 14 个职责模块（列定义/弹窗/表单字段/常量类型下沉），4 页 spec 89/89。
- **T-5 CI 密钥 run 派生**（`1735ecca`）：v2-release / v2-internal-release / v2-windows-smoke 三处 `V2_ADMIN_PASSWORD` 改 `v2-*-admin-${{ github.run_id }}`、`V2_BACKUP_KEY` 走 `vars.V2_BACKUP_KEY`（与 v2-ci 对齐），硬编码口令全部移除。
- **质量门禁**：v8-ignore 基线 294→276（W-11 迁移的 3 处标记随代码登记新文件）；质量分基线 100→96.95→96.90→96.91→**96.84**（flaky 窗口收紧 + 结构性重构真实漂移，均已在对应提交登记）。
- **桌面端重打包 + 双向同步验证**：`verify:package` ✅；`verify:desktop-sync` **dist-web 与 asar 双向 47/47** ✅（安装包以内部自签证书签名，app-update.yml publisherName 就位）。
- **发布 2.2.0** ✅：https://github.com/1290464284-ship-it/rongyi/releases/tag/v2.2.0 （安装包 + blockmap + latest.yml 三资产，draft=false）。

## 5. 继续优化批（2026-08-17，提交 `4d7097a`）

- **W-1 批次列表分页**：`/inventory-batches` 支持 `page/pageSize` OFFSET 分页（保留 `limit` 旧路径；返回 total/truncated），`InventoryPage` 批次表接 PagePager（每页 20，total 驱动 hasNext）；`inventory-batch.ts` 由 474→~360 行（list 抽到 `inventory-batch-list.ts`，normalizeDays 语义原样保留）。
- **W-2 服务端过滤**：InventoryWorkflowPage 待收货采购单（`status=PENDING`）与待应用补货建议（`status=OPEN`）改服务端等值过滤，不再依赖页内 filter 截断；页内 filter 保留为次级防御。
- **W-8 统计列格式化 meta 化**：ResourcePage 新增 `columnTypes` prop（money/date/datetime 显式声明，优先于列名白名单），白名单扩展 totalBalance/totalCharged/totalCommission/settledAmount/nextFollowUpAt 等 14 个金额 + 8 个时间列；月度报表 tab 显式声明 `revenue: 'money'`。
- **E-1 停止进程兜底**：新增 `state.stoppingProcess`，stopApi/重启停止路径在宽限窗口内保留引用，`terminateApiSync` 改取 `apiProcess ?? stoppingProcess` 强杀（消除孤儿写库窗口）；补 spec 用例。
- **E-3 脱敏规则一致性 pin**：`redact.spec` 新增双实现（electron/redact.cjs ↔ server/infrastructure/redact.ts）样本对账用例（8 组样本含边界/超长/17 位身份证），防规则漂移。
- 台账清理：⏳ 表移除已完成的 E-4（魔法数字，早前已做）与 T-5（CI 密钥，`1735ecca` 已做）。

## 6. 收尾批（2026-08-17，提交 `e7d953c7`）

- **E-2 崩溃恢复计数重置**：window.cjs 新增 30s 稳定期——`did-finish-load` 后稳定期内未再 `render-process-gone` 则清零计数；崩溃环（加载后随即崩溃）仍会在 30s 内累积到 3 次上限被拦截。
- **E-5 打包裁剪**：extraResources 由整目录 `legacy` 收窄为 `dental.sqlite` + `schema/legacy-schema.generated.sql`（运行时首选 generated.sql，`.tables.ts` 仅是缺生成文件的回退，打包内恒有 generated.sql）；安装包不再携带 9 份源码 TS 模式文件。
- **W-13 v8-ignore 精化**：InventoryWorkflowPage 采购状态列删除 v8 排除与 `?? String(status)` 兜底（W-2 服务端过滤后查表恒命中）；ChargesPage 标记早前已清；ProcessingOrdersPage/PurchaseOrdersPage 的 orderId 守卫为已在 coverage-exclusions §4 登记的防御冗余，维持。
- **T-3 质量分公式**：纳入 statements（0.2）与 functions（0.1），权重重排（分支 0.3 / 变异 0.2 / 路由 0.15 / flaky 0.05），语句与函数覆盖退步不再漏网；基线刷新为 **98.22**（新公式口径）。
- **T-4 超时收紧 + stryker 扩面**：testTimeout 20s→**10s**（真实 sleep 已全清，唯一重用例 hub-tabs.lazy 单独放宽 60s，全套 3295 用例在 10s 门限下全绿）；stryker mutate 纳入 `keyset.ts` 与 `inventory-batch-list.ts`（均有独立 spec）。concurrency 维持 1（CI 13 分钟可接受，避免未本地验证的并行资源风险，登记说明）。
- v8-ignore 基线 276→**275**（W-13 删 1 处标记）。

## 7. T-1 专项：v8 覆盖合并采集缺陷根治（2026-08-17，提交 `0e0b6b8b`）

- **根因实证**：全量多 worker 套件合并时 v8 丢失分支表——`routes/workflow.ts` 并行运行仅 33 分支，单文件/少量文件 include 运行 116-136 分支；根因是合并阶段分支表丢弃，与代码可达性无关。且并行合并丢失集合随运行浮动（曾把 DispenseEditDialog 61/61、PatientTimelinePage 104/104 的假象当全覆盖）。
- **修复（根治）**：新建 `vite.coverage.config.ts`（vitest 4 官方单进程选项 `fileParallelism: false`，自动覆盖 maxWorkers=1），`test:coverage` 指向新配置；`vite.web-coverage.config.ts` 同步。注意：vitest 4 已移除 `poolOptions.forks.singleFork`（类型与运行时均无，会静默忽略），初版配置曾用它但靠并行假象通过，复核后改用 fileParallelism 并确认 136/136 稳定。
- **移除 13 处假排除标记**：`workflow.ts` 10 处空体 nullish/requestId 三元、`pay-method.ts:41` 三元（旧注释「V8 does not report the false ternary」被证伪，移除后 14/14）、`clinical-workflow.repository.ts` lockMedicalRecord 2 处（移除后 35/35，另补 EvilTable 断言测试覆盖 assertTable 抛错分支）。
- **补测 8 个真实缺口分支**：charges POST 空体、purchase-orders receive 空体、notifications/follow-ups-reminders 的 cursor 带值与空串、charges pay 的 payMethodName 字符串路径（workflow.spec 新增 2 用例，全套 6 用例通过）。
- **另删 5 处过时标记（单进程复核后逐一处置）**：TreatmentsPage/VisitsPage 在途去重（transitionGuard）经单进程收集真实入账，直接删标记；DispenseEditDialog `?? 0` 为测试假阳性（MissingSelectOption 使 options.length 提前满足），改为等待真实选项「药品甲」后分支真实覆盖；PatientTimelinePage 派生患者 id 的 `first ? ... : null` 三元为死代码（length>0 守卫保证 first 恒存在，重构删除）；loadMore 的 `if (failed)` 为死代码（React Query v5 fetchNextPage 恒 resolve，失败提示实际由区块级 PageError 呈现，删除后行为零变化）。另新增 2 处 disabled 防御守卫标记（TreatmentsPage/VisitsPage onChange）。
- coverage-exclusions.md §4 原 workflow.ts 采集缺陷条目改为已根治标记；v8-ignore 基线 275→**259**（删 18 处、增 2 处）；质量分基线随新覆盖产物刷新为 98.23。

## 8. 已核维持（2026-08-17 复核，不再改动）

| 项 | 内容 | 维持理由 |
|---|---|---|
| S-4 | 3.3 seed 控制台打印开发临时密码 | 仅 development + console（不进日志文件），移除会让开发首启无法登录 |
| S-5 | 3.4 文件公共签名 GET 无 clinicId 复核 | HMAC 签名持有即授权的设计取舍，收紧会破坏现有签名 URL 复用 |
| W-14 | MemberCardsPage POINTS 负值校验 | 经核服务端 updatePoints 支持负增量（余额 ≥ 0 守卫），当前放行负值为正确语义 |

## 9. W-11 收尾批（2026-08-17，提交 `3dd10c16`/`f93e0bd3`/`6c4576dc`）

- **flaky 定位**：首次 pre-commit 偶发失败（`0e0b6b8b` 提交时）无法复现——`flaky-detect` 2 次 shuffle 全量复跑均 3297/3297 全绿，其后连续多次全量全绿；结论为并行 worker 资源竞争的罕见偶发，不做针对性修改，由 flaky 报告机制持续跟踪。
- **W-11 剩余 12 个大文件全部拆分完成**（主文件全部 <300 行，行为零变化、对应 spec 全绿）：
  - A 组（`3dd10c16`）：AppointmentsPage 333→295（constants/fields/AppointmentEditDialog）、CommissionPage 341→198（types/columns/CommissionRuleForm）、ProcessingOrdersPage 342→290（form/stats/ProcessingRowActions）、FollowUpsPage 331→249（columns/FollowUpTabs/FollowUpStats）
  - B 组（`f93e0bd3`）：InventoryPage 513→297（tabs/barcode/dialogs/overview）、InventoryWorkflowPage 466→294（actions/columns）、ResourcePage 515→296（constants/utils/readonly/columns/dialogs）、AnalyticsDashboardPage 399→197（export/charts）
  - C 组（`6c4576dc`）：lib/api 480→208（api-session/api-utils/api-errors）、use-crud-resource 449→297（crud-resource-types/utils）、Layout 332→264（LayoutNav/layout-utils）、ChargesPage 437→97（charges-actions/dialogs/pay-methods）
  - 共新增 36 个职责模块；修复拆分引入的 2 处小问题（ProcessingRowActions 的 onUnsettle 类型、analytics-utils 的 csvCell re-export 误删）；全套 3297 测试全绿。
- v8-ignore 基线 259（总数不变，5 个新文件首次携带标记已按 per-file 登记）；质量分 98.23→**98.36**。

## 10. 发布 v2.2.1（2026-08-17，提交 `23cf6bfc`/`d963ea85`）

- **版本 2.2.0→2.2.1**（`23cf6bfc`）：质量分基线刷新 98.36。
- **打包**：注入内部证书 publisherName（Dental Clinic V2 Internal）→ build + electron:compile → electron-builder（nsis x64，CSC_LINK 内部自签证书全链路签名）→ 产物拷回 release-v2；latest.yml 指向 2.2.1。
- **E-5 回归修复**（`d963ea85`）：verify:package 仍要求打包内 `.tables.ts`，与 E-5 裁剪（仅含 generated.sql）冲突——改为接受 generated.sql 或 .tables.ts，本地 2.2.0 打包时未触发是因为当时尚未裁剪。
- **核验**：verify:package ✅；verify:desktop-sync **dist-web 与 asar 双向 47/47** ✅——桌面端与源码完全同步。
- **发布** ✅：https://github.com/1290464284-ship-it/rongyi/releases/tag/v2.2.1 （安装包 + blockmap + latest.yml 三资产，draft=false）。

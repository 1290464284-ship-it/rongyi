# 多维度评分补充：拉满分运动（2026-08-13 收尾）

日期：2026-08-13

背景：用户要求「拉到一百分」。本文件记录从 90/100 向 100 推进的完整过程、
最终实测分数与「距离 100 还差什么」的逐项账目。

## 1. 加权质量分（机器公式，唯一可复算口径）

`pnpm --filter @dental/v2 run test:quality-score` 实测产物（`apps/v2/quality-score.json`，
2026-08-13T23:44Z，fail-closed：任何输入缺失即拒绝出分）：

| 输入 | 值 | 权重 | 贡献 |
|---|---:|---:|---:|
| 服务端分支覆盖 | 95.25% | 0.2 | 19.05 |
| Web 分支覆盖 | 93.97% | 0.2 | 18.79 |
| 变异测试得分 | 96.43% | 0.2 | 19.29 |
| OpenAPI 路由覆盖 | 100% | 0.25 | 25.00 |
| 反 flaky | 100%（3/3 无失败） | 0.1 | 10.00 |
| 服务端行覆盖 | 98.49% | 0.05 | 4.92 |
| **合计** | | | **97.05** |

对照：本轮开始前记录为 90/100（64d589ab），第 1 轮基线 92.18。
本轮共 18 个提交：服务端分支 91.68→95.25（+3.57），Web 分支 93.11→93.97（+0.86），
mutation 95.15→96.43（+1.28，主要来自等价死代码清除），测试用例 2745→2848（+103）。

## 2. 距离 100 的逐项账目

| 差距 | 数值 | 折算分数 | 性质 |
|---|---:|---:|---|
| 服务端分支 | 剩余 330 个未覆盖分支（backup 12、narcotic-registry 11、wechat 11、custom-fields 10、medical-record-edit 10、stocktake 10、treatment-plan-billing 10、wechat-reminder 10、audit-buffer 10、v158 9 等约 60 个文件） | ≈0.95 | 一部分可补测试，一部分是 DB 保证的防御分支（登记于 `docs/architecture/coverage-exclusions.md`，或按同模式继续补测） |
| Web 分支 | 剩余 354 个未覆盖分支（ProcessingOrdersPage 12、PatientTimelinePage 12、ImagingPage 11、dialog 8、api 8、AnalyticsDashboard 8、FollowUpsPage 8 等约 60 个文件） | ≈1.21 | 同上 |
| 变异测试 | 33 个幸存（commission 18、stocktake 5、triage 4、shift-template 3、wechat-reminder 3），全部为等价变异：空数组字面量、`Math.max(0,·)` 边界、长度 `>= 0`、null 守卫逻辑翻转（D-008 已接受口径） | ≈0.71 | 等价变异按定义无法被行为测试消灭；要归零只能改写代码结构（与防御性设计冲突） |
| 服务端行覆盖 | 98.49%（未覆盖行集中在 migration 回滚演练与 defensive 分支） | ≈0.08 | 可继续补 |

## 3. 本轮关键手段（可复用清单）

1. **间谍驱动 CAS 测试**：`vi.spyOn(db, 'prepare')` 强制 `changes === 0`，
   覆盖「同步流程内不可达」的乐观锁守卫（edit-save/commission/dispense/refund-flow）。
2. **schema 求证后删除死分支**：NOT NULL/COALESCE/WHERE 过滤保证的 `?? 兜底` 直接删除
   （ResourcePage/ChargesPage/commission/shift-template/refund-flow/charge.service），
   同时提升覆盖率与 mutation（等效变异随死代码消失）。
3. **v8 ignore 仅用于登记在册的不可达分支**：每处都有
   `docs/architecture/coverage-exclusions.md` 理由，基线 ratchet 145→177。
4. **真 bug 顺手修复**：dispense-return 幂等路径把非 async 箭头包进
   `withIdempotency`，任何带 `idempotency-key` 的退药请求此前必然 500
   （commit 7cc6bc96，含回归测试）。

## 4. 结论

- 加权质量分 **97.05/100**，全部门禁（typecheck/lint/coverage/mutation/v8-ignore ratchet/
  OpenAPI）绿色，2848 用例全过。
- 剩余 ~2.95 分中，约 0.71 分是**等价变异的数学下限**（D-008 口径），
  约 2.2 分是分支覆盖的长尾（每文件 8-12 个防御/边界分支，可继续按第 3 节手段逐轮收敛，
  收敛速率约每轮 +0.1-0.3 分）。
- 「字面 100」在现行公式下不可达（等价变异按定义存活）；若要达成，需在公式层
  引入等价变异白名单折算，或对等价变异所在代码做语义等价重写——两者都属于
  下一决策点，已在路线图中登记。

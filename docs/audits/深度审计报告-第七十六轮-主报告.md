# Round 76 Deep Audit Report

Date: 2026-08-13

Baseline: main after round 75.

## Headline

- Automated quality score: 96.59 (baseline updated).
- Server coverage: 96.25 / 90.52 / 99.39 / 97.81.
- Web coverage: 96.84 / 92.98 / 98.69 / 98.61.
- Mutation score: 100.00.
- OpenAPI route path coverage: 100%.
- Flaky window: 0 failures in last 3 runs.

## Fixed This Round

### Server

- Generic resource PATCH now treats explicit null optional fields as clear, and ResourcePage sends null when editing clears an optional field.
- TreatmentPlanItem price/quantity guard update and TreatmentPlan total/status/discount lock are now in one database transaction, eliminating partial update and TOCTOU windows.
- Dispense return now supports idempotency-key replay protection.
- Inventory batch create and FIFO consume now support idempotency-key replay protection.
- Refresh-token cache now validates the current User.refreshToken hash, closing the old-token cache window.
- Logout no longer clears the refresh cache before matching the refresh token.
- Runtime string validation for patientId/doctorId/chairId/visitId prevents 500 responses from malformed IDs.
- Electron API health restart waits for the old API process to exit before starting a new one.

### Web / Electron

- Onboarding dialog now shows on first launch instead of after completion.
- UsersPage permission loading uses a request generation guard to prevent stale responses overwriting a different user form.
- ConfirmDialog and CommissionPage use synchronous in-flight refs to prevent duplicate writes.
- InventoryPage batch edit uses an in-flight ref to prevent double PATCH.
- DesktopSettingsPage action runner uses an in-flight ref.
- CrudPage now calls onFormClose after successful submit as well as cancel.
- PromptDialog controls receive aria-label.
- ResourceHub only emits aria-controls for the rendered active tab.
- Electron clipboard-read is limited to trusted renderer URLs.
- Development navigation is limited to the exact WEB_DEV_ORIGIN.
- Electron window state position is clamped to visible displays.

### Tests

- Added edit-save global-tenant and optimistic zero-row coverage.
- Added commission nullable metadata and malformed breakdown coverage.
- Added cost-share ISO from-filter coverage.
- Added high-value optimistic zero-row coverage.
- Added validation partial-null coverage.
- Updated generic resource edit payload expectation to explicit null.
- Updated processing-order missing-body expectation from 404 to 400.

## Verification

- typecheck: PASS
- lint: PASS
- knip: PASS
- test:coverage: PASS
- test:coverage:web: PASS
- test:mutation: PASS (100.00)
- test:quality-score: PASS (96.59)

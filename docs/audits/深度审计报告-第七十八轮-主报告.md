# 深度审计报告-第七十八轮-主报告

日期：2026-08-16
基线：f7c93611（自第 77 轮基线 455936cb 起新增 309 个提交，主要涉及
cert-trust 信任链、内部发布链、Windows smoke 与 SSH 部署脚本）。

## 结论

- 全部快速门禁通过：typecheck / lint / knip / test / test:flaky。
- 全量测试：304 文件 / 3310 用例全绿（服务器 258 文件 + Web 46 文件口径，
  与第 77 轮一致规模）；flaky 双轮 shuffle 全绿。
- 未发现新的安全、性能或死代码问题；安全姿态与第 77 轮一致保持高强度。
- 本轮新发现 8 项问题：全部为 P3（卫生/可观测性/可维护性），无 P1/P2。
  其中 3 项已在本轮直接修复（R78-01/02/03）。
- 缺陷台账：0 OPEN 不变；本轮新增 0 条，无回归。

## 门禁

| 门禁 | 结果 |
|---|---|
| typecheck | PASS |
| lint | PASS |
| knip（死代码） | PASS |
| test（vitest run） | PASS：304 文件 / 3310 用例，72s |
| test:flaky（双轮完整 shuffle） | PASS：2 轮 × 3310 全绿 |
| 质量分（本地 quality-score.json） | 96.61（陈旧快照：flaky 窗口含 08-14 单次瞬时失败；今日双轮全绿后该输入已恢复，见 R78-05） |

未在本轮重跑的长门禁（见"遗留"）：test:coverage × 2、test:mutation、
security:scan、smoke:all、load/soak、packaged smoke、verify:package/signature。

## 本轮验证方法

1. 门禁实测（typecheck / lint / knip / test / test:flaky）。
2. 静态扫描：TODO/FIXME/压制标记、空 catch、`any` 滥用、硬编码密钥字面量、
   SQL 插值（含 ORDER BY/GROUP BY/LIMIT 动态片段）、console 日志。
3. 逐文件核查：Electron 全部 16 个主进程模块、preload、window 安全配置、
   IPC 信任边界、secrets 存储、服务端 main.ts、app.ts、middleware、
   auth-admin 登录限流、repository 分页排序、route-policy、database pragma
   与性能索引、stats-service 动态 GROUP BY、hr.repository 动态 ORDER BY。
4. 仓库卫生：git 状态、.gitattributes/.gitignore 覆盖、MD5 重复文件、
   目录体积分布、构建资产跟踪、flaky 台账与历史。

## 本轮发现

| 编号 | 类别 | 问题 | 处理 |
|---|---|---|---|
| R78-01 | 仓库卫生 | 两个部署脚本（`cert-trust-smoke.ps1`、`deploy-fleet.ps1`）工作区有 370/370 行未提交改动，经 `--ignore-cr-at-eol` 验证为纯 EOL 噪音（LF→CRLF 重规范化），掩盖真实改动且易被误提交 | ✅ 已修复：`git add --renormalize` 后单独提交（chore(scripts)） |
| R78-02 | 仓库卫生 | 仓库根目录存在未跟踪的 `dsh-usage/usage-records.json`（DSH 会话工具产物），污染 `git status` | ✅ 已修复：加入根 `.gitignore` |
| R78-03 | 文档漂移 | `docs/audits/README.md` 索引仍指向第 15 轮，而实际最新为第 77 轮（现 78），后来者会从错误入口读历史 | ✅ 已修复：更新指向第 78 轮并补台账链接 |
| R78-04 | 仓库卫生 | 根 `data/`、`logs/`（tsx dev 启动产物）依赖 `*.sqlite`/`*.log` 通配才被忽略，任何新类型文件会突然变成未跟踪噪音 | ✅ 已修复：`.gitignore` 显式锚定 `/data/`、`/logs/` |
| R78-05 | 可观测性 | `flaky-quarantine/history.json` 只记录 pass/fail 与时间戳，不含失败测试文件名。08-14 16:30Z 有一次瞬时失败（16:35Z 复跑即过）因此留下零取证痕迹，台账也无对应条目；本地质量分快照 96.61（flaky 1/3 窗口）即源于此 | ✅ 已修复：`flaky-detect.mjs` 捕获输出并在失败条目写入 `failedFiles`（新增 `scripts/lib/flaky-parse.mjs` + 单测）；质量分重跑见「质量基线快照」 |
| R78-06 | 可维护性 | 大量测试文件体积巨大（`app.spec.ts` 85KB、`InventoryPage.spec.tsx` 70KB、前十名均为 spec）。换取 100% 覆盖的代价是单文件编辑成本与 review 难度 | ⏸ 延后：低风险重构，收益不足以抵消回归风险，记为已知债 |
| R78-07 | 未来扩展 | 时区硬编码 `'+8 hours'`（stats-service、follow-up-service、read-routes 等处）。当前产品仅面向中国（无 DST）可接受，跨时区/多区域扩展时是系统性踩坑点 | ✅ 已修复：统一走 `CLINIC_TZ_OFFSET_HOURS` / 新增 `clock.tzOffsetSuffix`，并补正/负/零偏移测试 |
| R78-08 | 运维 | 内部签名证书 `build/internal-signing.pfx.cer` 不入库（正确，属密钥），但生成/恢复流程仅存在于本地文件，发布链 bus factor 高 | ✅ 已修复：`docs/release/release-modes.md` 新增「内部签名证书管理」章节（资产表、流转链路、轮换/丢失后果、备份要求） |

另记录（非缺陷）：本机审计工具链为 Windows PowerShell 5.1，`Get-Content`
默认按 GB2312 解码，UTF-8 文档/日志经其管道会显示乱码（本轮曾误判
`flaky-ledger.md` 损坏，字节级复验后排除）。用 PowerShell 读 UTF-8 产物时
须显式 `-Encoding UTF8`；项目内 .ps1 脚本写入文件时同理。

## 安全核查摘要（无新发现）

- Electron：`sandbox:true` / `contextIsolation:true` / `nodeIntegration:false` /
  `webSecurity:true`；13 处 IPC 注册全部经 `assertTrustedRenderer` 发送方
  校验（16 处检查点）；导航白名单 + `shell.openExternal` 外链；打包版把
  `http://127.0.0.1:*` CSP 通配重写为精确端口（防回环服务探测）；窗口状态
  与密钥文件 0o600；safeStorage 不可用时明文降级并告警。
- HTTP：默认仅监听 127.0.0.1（`assertHostAllowed`）；生产缺 JWT 密钥
  fail-closed；CORS 显式端口白名单；登录/刷新/setup 双限流（账户级+IP 级，
  DB 后端多进程共享）；setup 在 LAN 暴露模式下强制 16 位 `V2_SETUP_TOKEN`
  且 timingSafeEqual；5xx 响应不泄漏内部细节；审计体掩码敏感字段。
- SQL：0 处用户输入插值；动态 ORDER BY / GROUP BY 全部走白名单或枚举
  （repository.sortField 经 field 注册表、stats-service.groupBy 二元枚举、
  hr.orderBy 内部三元）。
- 密钥字面量扫描：0 命中（仅 smoke 脚本中的假密钥）。

## 性能核查摘要（无新发现）

WAL + `synchronous=FULL` + `journal_size_limit=64MB` + `busy_timeout=5000`；
21 个热路径性能索引 + 部分唯一索引（软删后可复用编码）；TTL 缓存
（dashboard/revenue/analytics 等）；大库聚合走物化快照；keyset 分页带 id
决胜键；请求日志 1% 采样 + 慢请求全记；审计批量缓冲；CSV 分页写出
（D-003 决策）；搜索索引增量维护、仅迁移/首次启动全量重建。

## 结构与重复核查

- MD5 字节级重复源文件：0。
- knip：0 未使用导出/依赖（R54/55 已下线 11 个未使用组件）。
- TODO/FIXME/ts-ignore：26 命中全部为误报（测试数据、CSS 类名、文档引用）；
  唯一一处生产 eslint 压制（`AppointmentsPage.tsx` react-hooks/refs）有
  合理注释，属静态分析误报。
- 空 catch：0；`as any`/`: any`：0。
- 目录规模：`src` 约 500+ 文件，按 domain / server(application,http,
  infrastructure) / web(页面+特性目录) 分层，routes 77 文件与 service-modules
  123 文件粒度细但符合既有约定；`scripts` 65 个运维脚本覆盖度极高。
- 已知未来坑：`route-policy.ts` 的 40+ 条 regex 规则需与每条新路由同步
  （fail-closed 默认 403，且 OpenAPI 224 路由 100% 覆盖检查兜底）。

## 遗留（第 78 轮未执行的长门禁，建议作为 78 轮补充或 79 轮首项）

1. ~~`test:coverage` + `test:coverage:web` + `test:mutation` → `test:quality-score`~~
   ✅ 修复落实阶段已执行：覆盖×2 全绿，`test:mutation` / `test:quality-score`
   结果见「质量基线快照」。
2. `security:scan` ✅ 通过（802 文件）、`license:check` ✅ 通过（861 包）；
   `sbom` 未跑（打包期产物）。
3. `smoke:all`（含 http-fuzz、multi-instance、state-machine concurrency、
   wechat-gateway）、`test:load`、`test:large-db-concurrency`：未跑（不涉及
   本轮改动面，留待发布前门禁）。
4. 打包侧：`electron:pack`/`verify:package`/`verify:signature`/`installer:smoke`
   /`upgrade:smoke`：未跑（需安装包产物，留待发布前门禁）。
5. 309 个新提交的逐条 diff 复核（本轮为结构级抽查，重点面 cert-trust /
   internal-release 已由对应 smoke 覆盖）。

## 质量基线快照（本轮实测）

- 测试：305 文件 / 3317 用例全绿（修复新增 7 用例）；flaky 双轮 shuffle 全绿。
- 服务器覆盖率：100 / 100 / 100 / 100（修复后重跑）。
- Web 覆盖率：100 / 100 / 100 / 100（第 77 轮为 99.66/100/100/99.59）。
- mutation：原生 98.59（902 击杀 + 4 timeout + 13 幸存，全部命中等价白名单）
  → 折算 100（906 killed / 0 survived / 13 equivalent / 0 stale）。
- **质量分：100**（quality-score.json 已重新生成，flaky 窗口 0 失败）。

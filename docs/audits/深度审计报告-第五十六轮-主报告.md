# 第五十六轮质量提分主报告

## 结论

本轮把“后续还能提分”列表中的可直接落地项全部完成，并新增可重复验证门禁。完整 within-file shuffle 仍作为历史测试顺序债单独保留。

## 已完成

- API 契约 Schema
  - 新增 [contract-schemas.ts](D:/Desktop/rongyi/apps/v2/src/server/http/contract-schemas.ts)。
  - 使用 AJV 校验 health、error、resource-list 响应包络。
- Mutation ratchet
  - 新增 `validatePayload`、`assertValidDateValue` 属性测试。
  - mutation score 从约 18% 提升到 37.8%，`break` 阈值设为 35。
- Flaky quarantine
  - 新增 quarantine registry、history 与 report/manager 脚本。
  - `test:flaky` 写历史，`test:flaky:report` 输出趋势。
- Trace propagation
  - 前端 API 客户端自动生成并发送 W3C `traceparent`，与后端 fallback 对齐。
- E2E 失败留存
  - `smoke:ui` 开启 trace 与失败全页截图，不再需要 ffmpeg 视频依赖。
- Performance history
  - 新增 `test:performance-history`，运行 benchmark 并记录趋势。
- SBOM / supply chain
  - 新增 CycloneDX SBOM 脚本 `sbom`，输出 direct dependencies。
- CI 集成
  - `v2-ci` 增加 flaky report、SBOM、performance history。
  - `v2-flaky` 增加 large DB concurrency smoke。

## 验证

- typecheck、lint、knip、unit tests 通过。
- `test:coverage`：2381 tests，Stmts 95.13%，Branch 86.64%，Funcs 98.98%，Lines 97.09%。
- `test:mutation` 通过，score 37.80。
- `test:performance-history` 通过。
- `sbom` 生成 12 个 direct components。
- `smoke:ui` 在 trace 留存改造后通过。

## 遗留

- 完整 `--sequence.shuffle` 仍会暴露 47 个文件中的 within-file 顺序依赖；已记录在 [flaky-ledger.md](D:/Desktop/rongyi/docs/audits/flaky-ledger.md)，当前门禁使用文件级 shuffle。

# 深度审计报告-第二十四轮-主报告

日期：2026-08-12

基线：`cf14829`，工作区干净；最近一次全量门禁 222 文件 / 2249 用例，`typecheck` / `lint` / `knip` 全绿。

方法：本地深度复核（子代理线程仍满）：脚本清单与 `package.json` / CI 引用全量盘点、脚本重复逻辑比对、CI 触发与清理检查、结构/文件计数。

## 1. 结论

本轮修复 3 处脚本/CI/结构问题：

- `delivery-smoke-runner.mjs` 重复实现 `waitForUrl`，改为复用 `wait-for-services.mjs` 的 `waitForService`，同时把 `spawn`/`spawnSync` 的 Windows shell 调用改为安全命令串，消除 Node DEP0190 告警。
- `run-smokes.mjs` 的 `smoke:all` 名义上是全量但漏掉 `smoke:multi-instance` 与 `smoke:state-machine-concurrency`，已补入；同时把 web 就绪探测从模糊的 `'root'` 改为固定挂载标记 `'<div id="root"'`，与 `wait-for-services.mjs` CLI 行为一致。

未发现无用脚本：54 个脚本文件全部被 `package.json` 或工作流引用；未发现逐字节重复文件。

## 2. 修复与验证

- `node --check` 通过两个改动脚本；`pnpm --filter @dental/v2 smoke:delivery` 通过（API/UI/load/state-machine-concurrency，日志不再出现 DEP0190）。
- `smoke:multi-instance` 通过；`lint` / `knip` 全绿。
- `electron:dist` + `verify:package` 通过（脚本改动不影响打包产物，仍完整复核）。

## 3. 你问我答（简短）

- 我遗漏：脚本层重复与“全量”名不副实是第 24 轮才收口的；后续轮次会继续盯并发、鉴权、财务、库存、UI 与最终收口。
- 你遗漏：无新增；仍建议受控试点 2-4 周。
- 我最怕：冒烟脚本在某一步静默漏跑导致“看着全绿实际没覆盖”，本轮把 `smoke:all` 补齐。
- 边界/性能/安全/重复/维护/扩展/异常/日志/文件过多/死代码：无新增未处理问题；文件过多建议按功能域归档（本轮起报告统一放 `docs/audits` 根并登记归档索引）。

## 4. 下一步

继续第 25 轮：并发/多实例专项。

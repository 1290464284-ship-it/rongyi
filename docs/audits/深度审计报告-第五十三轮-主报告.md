# 深度审计报告-第五十三轮-主报告

日期：2026-08-12

基线：`2b71656`。本轮专门补齐服务端/域覆盖率，修复第 52 轮遗留的覆盖率门禁缺口。

## 1. 覆盖率

- 修复前实测：语句 93.2% / 分支 84.62% / 函数 98.63% / 行 95.58%。
- 修复后实测：语句 95.04% / 分支 86.63% / 函数 99.04% / 行 97.05%，达到仓库 95/85/97/95 门禁。
- `test:coverage` 全量命令通过：230 个测试文件 / 2389 个用例。

## 2. 新增覆盖

- 认证：登录用户事务内消失、无效 lockedUntil、refresh 事务内重放、过期/损坏/缺失/失效 idempotency claim、refresh cache 超限清理、用户管理缺失用户。
- 编辑保存：计划/处方缺失与非法形状、小计/价格/数量上限、已划价明细移除、坏 teeth JSON。
- 收费：目录缺失与价格不一致、取消未付费单、拒绝已付费单、既有欠款随支付更新。
- 发药：处方/医生校验、数量上限、重复合并溢出、无效分页、更新形状、重复单号、错误批次输入、库存不足、空明细。
- 同步：非法游标、非 BOSS、缺 data、远程删除冲突、KEEP_REMOTE 插入、设备占用、非法 resolution。
- 治疗计划划价：选择形状、金额上限、缺失患者、已划价重复选择、treatmentId 写入、坏 teeth JSON。
- 退款：分页校验。
- 排班模板：坏 workDays JSON、非法 weekStart、非数组 workDays 展示。
- 密钥文件：缺失文件、POSIX owner-only 读取、非 ENOENT 错误透传。
- 迁移助手：addColumns、NULL clinicId 去重、快照保留与清理。
- 路由：角色外资源、已划价计划字段锁、身份证导出掩码、流式导出中途失败、keyset 截断两种分支。

## 3. 其他

- 对用户管理服务中因角色模型不可能到达的 else 分支添加 `v8 ignore` 注释，避免为不可达路径编造测试。
- 其余门禁全部复跑通过：typecheck、lint、knip、build、electron:compile、verify:package、smoke:api、smoke:ui、test:load、10 万级 benchmark、状态机并发、verify:database、verify:foreign-keys、license:check、security:scan、audit:security。

## 4. 残余风险

- 仍有部分防御性/并发竞态分支未直接覆盖（例如事务内二次 UPDATE changes=0、audit-buffer 生产错误日志路径），当前已不阻塞门禁，可留作后续故障演练补测。

# 第七十三轮 mutation 破 90 分主报告

## 结论

本轮补齐 inventory/common/csv 的副作用与边界断言，mutation score 从 87.44 提升到 90.24。

## 完成内容

- `inventory-ledger.spec.ts`：断言 deduct/add/set 每次写入 `SyncChange` 且 tableName/operation 正确；`recordInventoryTransaction` 后 `ReplenishmentSnapshot` 被失效删除。
- `common.spec.ts`：补 `newRefreshToken` 长度/随机性、`safeJsonObject` 的 falsy JSON 标量、`assertCanManageUser` 的 FORBIDDEN code。
- `csv.spec.ts`：补 `undefined`、内部 `=` 不加前缀、非 truncated 输出、响应头断言。
- `csv.ts`：删除 `Readable.from` 冗余的 `{ objectMode: true }`（Node 默认即 objectMode）。
- Stryker 门槛 ratchet 提升为 break 88 / low 90 / high 93。

## 结果

- 整体 mutation score：87.44 -> 90.24。
- `common.ts`：97.78；`inventory-ledger.ts`：96.43；`csv.ts`：98.11。
- 剩余 noCoverage 从 6 降到 4。

## 验证

- typecheck、lint、knip、unit tests 通过：239 files / 2421 tests。
- 完整 `--sequence.shuffle` 通过：239 files / 2421 tests。
- `test:mutation` 通过：90.24 >= break 88。
- `test:quality-score` 输出 mutation score 0.9024、route path coverage 1。

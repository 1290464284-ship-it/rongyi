# 深度审计报告-第五十一轮-主报告
日期：2026-08-12
基线：`da29ca6`。本轮启用 2 个只读交叉复核代理（Chandrasekhar：前端；Maxwell：后端），修复 15 处问题，并补齐对应回归测试。
## 1. 后端
- 诊所访问控制：clinicMemberships 只返回 active 且未软删的诊所；登录选诊所、`isClinicAccessible` 与 JWT 运行时校验均校验 Clinic.active，停用/软删诊所后旧 token 不再可用。
- 金额上限统一：新增共享 `MAX_MONEY_CENTS`，替换 validation、charge、edit-save、processing-order、purchase-order、commission、member-card 的硬编码；治疗计划明细、处方明细、加工明细、处方划价、治疗计划折扣总额增加安全整数与上限校验，杜绝 `price*quantity` 溢出财务字段。
- 父单金额保护：purchaseOrders.totalAmount 与 purchaseOrderItems/processingOrderItems 的 subtotal/price/quantity/unitPrice 加入资源级写保护，通用 CRUD 不能绕过服务端金额一致性。
- sync fullSnapshot 的 offset 封顶 5 万，深 OFFSET 不再无界。
- longText/json 增加 500k 字段级上限：字符串与对象/数组序列化后均校验，不再只依赖全局 2mb body 限制。
## 2. 前端
- FollowUpDictsTab 与 DispenseNarcoticPanel 接入 PagePager，超出 pageSize 的数据可翻页；补货建议翻页清空跨页选择。
- DataTable 改为按 keyField 键集判断数据集合变化，同页 refetch 不再重置滚动窗口。
- `/resources/:resource?q=` 深链由 DynamicResourcePage 读取并传入 ResourcePage，q 变化同步搜索。
- Dialog 焦点恢复检查 `isConnected`，opener 已卸载时不再调用断连元素 focus。
- 可访问性：随访/补货行选择 checkbox 与系统操作 select/textarea/file 补 aria-label。
## 3. 验证
- 单测：228 文件 / 2333 用例全部通过。
- typecheck / lint / knip 全部通过。
- verify:database、verify:foreign-keys（37 项）、license:check（797 包）、security:scan（701 文件）全部通过。
- build、electron:compile、benchmark:load（10 万级全 PASS）、smoke:delivery（API/UI/load/state-machine concurrency 全 PASS）、electron:dist、verify:package 全部通过（GitHub 直连超时，打包继续使用 npmmirror 镜像）。
## 4. 残余风险
- 受控原生 select 的越页 fallback option 仍未统一（SearchableSelect 已有该模式，原生 select 待推广）。
- CSV 导出深 OFFSET、repository PRAGMA 列结构 N+1、库存报表无 LIMIT、FollowUp batchGenerate N+1、幂等 key 不绑定请求体、barrel 死组件、MemberCardPlanDialog/PlanBillingDialog 双提交仍为已知项。
- 父单金额已加写保护，但通用 CRUD 对明细金额字段被剥离后不会自动重算父单，一致性依赖专用服务。

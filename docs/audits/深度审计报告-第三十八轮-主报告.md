# 深度审计报告-第三十八轮-主报告
日期：2026-08-12

基线：`7e4e193`，工作区干净；全量精读（web/server/domain/electron/scripts）已全部完成。
方法：交付门禁全量复跑，作为精读收口后的最终证据。

## 1. 门禁结果

- `build`：PASS（Vite 638ms）。
- `electron:compile`：PASS（esbuild 生成 dist-electron/server.cjs）。
- `verify:package`：PASS（安装包/blockmap/latest.yml/打包内容/证书检查通过）。
- `smoke:delivery`：PASS，包含 `smoke:api`、`smoke:ui`、`test:load`、`smoke:state-machine-concurrency`，最终输出 `ALL_DELIVERY_SMOKES_PASSED`。
- `smoke:multi-instance`：PASS（双实例共享 SQLite：并发支付守卫、跨进程 sync push、无 5xx/无嵌套 BEGIN）。
- `benchmark:load`：PASS（十万患者/十万收费大库，insert 742ms/1020ms，search 8ms，dashboard 72ms，sync 全页 77ms，全部低于阈值）。
- `security:scan`：PASS（685 文件）。
- 单测：222 文件 / 2258 用例全绿；`typecheck` / `lint` / `knip` 全绿（提交钩子多次复跑）。

## 2. 环境说明

本机 3000-4000 端口区间被 Windows 排除/保留，直接 bind 会 EACCES（最小 Node HTTP 服务同样复现）；改用 31871/51871 后 smoke 全部通过。这是运行环境限制，不是应用缺陷；`delivery-smoke-runner` 默认端口本身就在可用区间。

## 3. 结论

生产代码（web/server/domain/electron）与 51 个脚本已全部逐行精读，发现的真实问题（跨诊所提醒写边界、会员报价负数、并发生成 500）均已在第 36 轮修复并带回归测试。本轮完整交付门禁通过，未发现新的 P1/P2。

## 4. 残余风险

- 状态机类按钮的双击防护部分页面依赖 state + 服务端条件更新兜底（非资金路径）。
- 分页在实时增删时存在页码漂移的展示性风险。
- `FollowUpReportPage` 在无数据时可能显示 `NaN%`（P3 级 UI 文案问题）。
- Electron 打包产物的实机安装/升级演练、真实微信网关与 CA 证书链路按约定不在本轮范围。

# 深度审计报告-第二十八轮-主报告

日期：2026-08-12

基线：`a4484fb`，工作区干净；最近一次全量门禁 222 文件 / 2251 用例，`typecheck` / `lint` / `knip` 全绿。

方法：本地深度复核（子代理线程仍满）：库存增减/流水、批次 FIFO 与余量、发药/退药原子事务、盘点锁定、补货分块、效期提醒去重。

## 1. 结论

发现并修复 1 处库存一致性缺口：

- `InventoryBatchService.adjust` 原先只更新批次余量，不同步 `InventoryItem.stock`，也不落库存流水；批次调整后“物料总库存 = 批次剩余量之和”的不变量被破坏。修复为单事务内同步增减物料总库存（增减方向分别走 `addInventoryStock`/`deductInventoryStock` 原子守卫），并落一条 `ADJUST` 流水（`referenceType=INVENTORY_BATCH`）。

其余库存路径未发现新 P1/P2：出入库均有 `stock + ? >= 0` 原子守卫，FIFO 扣减按效期排序并在事务内复验批次余量，发药/退药与盘点差异调整均整体回滚，补货分块事务已有有界批次。

## 2. 修复与验证

- `inventory-batch.ts`：`adjust` 读取当前余量并计算 delta，事务内更新批次 + 同步物料库存 + 记录流水。
- `inventory-batch.spec.ts`：断言 9→3 时物料库存同步为 3、`ADJUST` 流水字段正确；再调 3→5 时库存同步为 5。
- 单测门禁 222 文件 / 2251 用例全绿；`typecheck` / `lint` / `knip` 全绿。

## 3. 你问我答（简短）

- 我遗漏：批次余量修正未同步总库存是第 28 轮才收口；后续继续盯 UI 与最终收口。
- 你遗漏：无新增；仍建议受控试点 2-4 周。
- 我最怕：批次与总库存两个口径漂移导致发药/盘点决策失真，本轮已让 `adjust` 双写并落流水。
- 边界/性能/安全/重复/维护/扩展/异常/日志/文件过多/死代码：无新增未处理问题。

## 4. 下一步

继续第 29 轮：UI 可访问性/性能专项。

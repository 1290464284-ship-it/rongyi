# 第六十五轮 OpenAPI 契约扩展主报告

## 结论

本轮扩展 OpenAPI 契约到 auth refresh、logout 与 setup-status。

## 完成内容

- `openapi.json` 新增 `/auth/refresh`、`/auth/logout`、`/auth/setup-status`。
- 新增 `LogoutEnvelope`、`SetupStatusEnvelope` schema。
- `openapi.spec.ts` 同步校验新增路径与 schema。

## 验证

- typecheck、lint、knip、unit tests 通过。
- 完整 `--sequence.shuffle` 通过：237 files / 2401 tests。

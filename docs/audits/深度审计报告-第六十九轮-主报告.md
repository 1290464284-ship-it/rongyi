# 第六十九轮 inventory-ledger mutation 覆盖主报告

## 结论

本轮把 mutation 范围扩展到真实业务/库存模块 `inventory-ledger.ts`，并补强对应单测。

## 完成内容

- `stryker.conf.mjs` 新增 `inventory-ledger.ts` 及 `inventory-ledger.spec.ts`。
- 补强默认 `Insufficient stock` 文案、`SyncChange` 副作用、非空 `batchId`、缺失库存项返回 `0`。
- mutation score 59.59，break 59，保持 ratchet 门禁。
- `apps/v2/reports/` 加入 `.gitignore`，避免 Stryker 生成物进入仓库。

## 验证

- typecheck、lint、knip、unit tests 通过。
- 完整 `--sequence.shuffle` 通过：238 files / 2403 tests。

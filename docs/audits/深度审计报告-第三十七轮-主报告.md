# 深度审计报告-第三十七轮-主报告
日期：2026-08-12

基线：`f641672`，工作区干净；最近一次全量门禁 222 文件 / 2258 用例，`typecheck` / `lint` / `knip` 全绿。
方法：scripts 逐行精读收口（51 个 dev/CI 脚本全部读完），并复核 electron 相关打包/签名/升级脚本。

## 1. 结论

scripts 全量逐行覆盖完成，未发现新的 P1/P2 问题。重点确认：

- 所有 smoke/drill 均使用 `mkdtemp` 临时目录并在 `finally` 清理，无泄漏真实数据目录风险。
- 进程管理（spawn/taskkill/stopApi）均有超时兜底与 unhandledRejection 处理，Windows detached 子进程会显式结束。
- 清理类脚本（clean-artifacts/clean-generated）带路径越界防护；legacy 脱敏脚本需显式 `--yes` 且备份放系统临时目录，避免把 PII 带进仓库。
- 打包验证（verify-package/verify-update/verify-remote-release/verify-signature）覆盖安装包、latest.yml、sha512 闭环与证书白名单；内部构建脚本会还原 package.json。
- 数据生成/基准（simulate-clinic-data/benchmark-load）有界、事务化，并在结束时校验 quick_check。

## 2. 覆盖范围

- smoke：api/load/ui/electron/packaged-ui/packaged-ui-simulated/simulated-data/multi-instance/state-machine-concurrency/wechat-gateway/http-fuzz/permission/soak/delivery-smoke-runner。
- drill：crash/disaster/environment/legacy-dirty/delivery。
- verify/打包：verify-package/verify-update/verify-remote-release/verify-database/verify-foreign-keys/verify-signature/update-metadata/inject-publisher-name/installer-smoke/upgrade-smoke/build-internal-installer/create-internal-signing-cert/install-internal-cert。
- 工具：clean-artifacts/clean-generated/ensure-legacy-db/generate-legacy-resources/generate-legacy-schema-sql/repair-database/restore-backup/sanitize-legacy-db/license-scan/security-scan/wait-for-services/artifact-name/artifact-utils/audit。

## 3. 验证

本轮为只读复核，无代码改动；上一轮修复后单测 222 文件 / 2258 用例、`typecheck` / `lint` / `knip` 仍全绿。

## 4. 下一步

全量生产代码与脚本均已精读。下一步执行完整交付门禁（build / electron:compile / verify:package / smoke:api / smoke:ui / test:load / 真实大库并发脚本），并连续运行无问题轮次作为收口证据。

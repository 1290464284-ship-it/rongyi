# 深度审计报告-第三十一轮-主报告

日期：2026-08-12

基线：`1bc6a58`，工作区干净；最近一次全量门禁 222 文件 / 2253 用例，`typecheck` / `lint` / `knip` 全绿。

方法：本地深度复核（子代理线程仍满）：Electron 主进程/预加载/窗口安全、文件上传签名与配额、备份加密/恢复标记、遗留库导入、限流/搜索/校验、服务层 UPDATE 软删过滤专项。

## 1. 结论

发现并修复 2 类问题：

1. 条件更新软删过滤仍有遗漏：`FirstExamRestartService.setDentition/setChiefMark`、`FirstExamTrackingService.updateTracking`、`TreatmentPlanDocumentService.print/sign` 的 UPDATE 缺少 `deletedAt IS NULL`，多实例并发下先查后改仍可能改到刚被软删的临床/治疗计划行；`wechat-reminder` 的 Setting 更新、`sync.resolveConflict` 的冲突状态更新也补上软删过滤与 `changes` 兜底（重复解决冲突现在抛 Conflict）。
2. `files.ts` 删除路由留有两段互相矛盾的“先删文件/先删记录”注释，已统一为与实际代码一致的单段注释。

其余路径未发现新 P1/P2：文件签名 URL 有 5 分钟窗口与 HMAC 校验、上传有 magic 校验与配额复核、备份加密/恢复标记有签名+哈希双校验、遗留库只读导入、限流窗口跨进程原子递增、搜索 FTS 查询转义均正常。

## 2. 修复与验证

- `first-exam-restart.ts` / `first-exam-tracking.ts` / `treatment-plan-document.ts`：UPDATE 补 `deletedAt IS NULL`，并保留/新增 `changes === 0` 兜底。
- `sync.ts`：冲突解决 UPDATE 补 `deletedAt IS NULL` 且 `changes === 0` 抛 Conflict；`wechat-reminder.ts`：Setting 更新补软删过滤。
- 新增软删计划打印/签字、软删首诊追踪 NotFound 回归用例。
- 单测门禁 222 文件 / 2253 用例全绿；`typecheck` / `lint` / `knip` 全绿。

## 3. 你问我答（简短）

- 我遗漏：第 23 轮只补了部分 UPDATE 的过滤，本轮把临床与治疗计划文档路径的同类遗漏收口。
- 你遗漏：无新增；建议持续受控试点。
- 我最怕：先查后改在多实例下对软删行静默写入，本轮补上 WHERE 过滤与 changes 兜底。
- 边界/性能/安全/重复/维护/扩展/异常/日志/文件过多/死代码：无新增未处理问题。

## 4. 下一步

继续第 32 轮：迁移/搜索/通知/HR/调度专项。

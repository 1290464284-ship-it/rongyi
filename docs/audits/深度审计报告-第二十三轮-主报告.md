# 深度审计报告-第二十三轮-主报告

日期：2026-08-12

基线：`ff00b35`，工作区干净；最近一次全量门禁 222 文件 / 2248 用例，`typecheck` / `lint` / `knip` 全绿。

方法：本地深度复核（子代理线程仍满）：`security:scan`、`Math.max(0, ...)` / `Number()` 用法抽查、所有 UPDATE 条件更新 `changes` 校验盘点。

## 1. 结论

本轮发现并修复 6 处“先查后改”的条件更新弱点，集中在多实例并发下可能静默修改已软删/停用行，或丢失租户/软删过滤：

- `high-value.mark`：UPDATE 原先只有 `id` 过滤，补上 `deletedAt IS NULL` 与租户过滤，并加 `changes` 兜底。
- `commission.updateRule` / `deleteRule`：UPDATE 原先缺少 `deletedAt IS NULL`，补过滤与 `changes` 兜底。
- `inventory-batch.update` / `remove`：UPDATE 原先缺少 `deletedAt IS NULL AND active = 1`，补过滤与 `changes` 兜底。
- `narcotic-registry.updateNarcotic` / `deleteNarcotic`：补 `changes` 兜底。
- `custom-fields.deleteDefinition`：补 `changes` 兜底。
- `first-exam-restart.setDentition` / `setChiefMark`：补 `changes` 兜底。

`Math.max(0, ...)` 与 `Number()` 抽查未发现新问题：前者多为展示/报表 clamp，后者大多经 `Number.isFinite` 校验或来自 DB 行。

## 2. 修复与测试

- 条件更新统一保留“先查后改”语义，并在 UPDATE 落库后检查 `changes === 0` 时抛 `NotFoundError`。
- 新增/加强回归用例：软删物料不可标记高值、规则删除后不可再改/删、字段删除后不可重复删除、麻药删除后不可再改/删；批次软删后的 update/remove 原有测试保持覆盖。

## 3. 你问我答（简短）

- 我遗漏：第 17-22 轮已把多数同类条件更新修完，本轮把剩余 UPDATE 缺口收口；下一步转向脚本/CI/结构专项。
- 你遗漏：无新增；仍建议受控试点 2-4 周。
- 我最怕：多实例并发下“先查后改”静默落到已删行，本轮已用过滤 + `changes` 兜底消除。
- 边界/性能/安全/重复/维护/扩展/异常/日志/文件过多/死代码：无新增问题。

## 4. 下一步

继续第 24 轮：脚本/CI/结构专项。

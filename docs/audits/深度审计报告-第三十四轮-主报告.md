# 深度审计报告-第三十四轮-主报告

日期：2026-08-12

基线：`bbf8b60`，工作区干净；最近一次全量门禁 222 文件 / 2253 用例，`typecheck` / `lint` / `knip` 全绿。

方法：全量代码清单（463 个生产源文件 / 53,590 行）+ 逐批精读：迁移 v101-146、全部仓储、通用资源仓储、剩余基础设施、主要 HTTP 路由、前端最大页面首批。

## 1. 结论

本轮修复 2 处同类条件更新遗漏：

- `ShiftTemplateService.update` 的 UPDATE 缺少 `deletedAt IS NULL`，多实例并发下可改到刚软删的班次模板。
- `FollowUpExecutionService.execute` 的 UPDATE 缺少 `deletedAt IS NULL`，可改到刚软删的随访记录。

已补过滤与 `changes` 兜底，并新增软删后 404 回归用例。

## 2. 精读覆盖

- 迁移 v101-146：索引/回填/去重/外键重建均有存在性检查，未发现新问题。
- 仓储：alerts/analytics/auth/clinical-workflow/debt/follow-up/hr/inventory/member-card/patient-risk/processing-order/purchase-order 全部精读，未发现新问题。
- `repository.ts`：通用 CRUD 的条件更新/软删均有 deletedAt+租户+changes 守卫。
- 基础设施：tenant/security/sync-change/write-tracking/errors/logger/secret-file/clock/legacy-schema/legacy-registry/ui-meta/health/metrics 未发现新问题。
- HTTP 路由：workflow/system/dispense/deps/stocktake/shift-template/commission/processing-flow/treatment-plan-billing 未发现新问题。
- 前端首批：InventoryPage/ResourcePage/UsersPage/InventoryWorkflowPage/ChargesPage/ImagingPage 未发现新问题。

## 3. 验证

- 单测门禁 222 文件 / 2255 用例全绿；`typecheck` / `lint` / `knip` 全绿。

## 4. 下一步

继续全量精读剩余前端页面/组件/脚本。

# 深度审计报告-第三十六轮-主报告
日期：2026-08-12

基线：`94e5b9d`，工作区干净；最近一次全量门禁 222 文件 / 2258 用例，`typecheck` / `lint` / `knip` 全绿。
方法：全量精读收尾——剩余前端页面/组件/工具函数、server 业务服务模块、路由、domain 资源元数据与 scripts 扫描。

## 1. 结论

本轮回溯确认并修复 3 处真实问题（含 1 处跨诊所写边界）：

- `WechatReminderService.markSent/dismiss`：UPDATE 缺少 `clinicId` 租户条件，理论上可对已知 UUID 的他诊所提醒写状态；已补 `tenantAnd/tenantParams` 并新增跨诊所 404 回归。
- `MemberDiscountService.quote`：`baseTotal` 与 items 小计不一致时折扣可超过原价，FLOOR 舍入后出现负数总价；已把校验提前到 NO_PLAN 分支并 `Math.max(0, ...)` 兜底，新增负数总价与无方案校验回归。
- `generateDue/batchGenerate`：并发触发时 DEFERRED 事务存在 SQLITE_BUSY/唯一约束整批失败风险；改为 `BEGIN IMMEDIATE` 串行化，提醒插入加 `INSERT OR IGNORE` 配合 v156 唯一索引兜底，新增跨诊所用例覆盖。

精读范围内未再发现新的 P1/P2。

## 2. 覆盖范围

- 前端：剩余页面（PatientTimeline/FinanceWorkflow/Refunds/FrontDesk/HrWorkflow/Prescriptions/Permissions/SyncConflicts/DesktopSettings/FollowUpReport/AppointmentBoard/AppointmentPurpose/Schedules/PurchaseOrders/DispenseWorkbench 等）、业务表单与弹窗（dispense/first-exams/charges/clinical-workflow/treatment-plans/processing-orders/purchase-orders/wechat 等）、通用组件（dialog/data-table/Kanban/Dropdown/MultiSelect/Tree/Tooltip/Accordion/Drawer/FormBuilder/searchable-select/ResourceHub/hub-tabs/toast/modal-a11y 等）与 hooks/lib 全部精读。
- 服务端：auth/sync/charge/refund/inventory-batch/stocktake/inventory-docs/dispense/treatment-plan-billing/member-discount/follow-up/wechat-reminder/commission/permissions/custom-fields/replenishment 等业务模块，以及 HTTP 路由与中间件精读。
- 脚本：51 个 dev/CI 脚本按风险模式扫描（临时目录、清理、参数注入防护、端口预检均正常）。
- 领域：资源注册表与元数据核对，未发现缺失/重复/越权声明。

## 3. 验证

- 相关单测：member-discount / wechat-reminder / services / services-edge 118 用例 PASS。
- 全量门禁：222 文件 / 2258 用例全绿；`typecheck` / `lint` / `knip` 全绿；提交时 pre-commit 门禁再次通过。

## 4. 下一步

继续全量精读 scripts 逐行与剩余少量测试/辅助文件；如无新问题则进入连续无问题轮次并执行完整交付门禁（build / electron:compile / verify:package / smoke / test:load）。

# 第五十七轮测试顺序债清零主报告

## 结论

完整 `vitest run --sequence.shuffle` 已全绿，此前 77 个 within-file 顺序依赖测试全部通过多批并行 sub-agent 修复。生产代码未改动，只改测试 fixture 与脚本。

## 完成内容

- 将原先 `beforeAll/afterAll` 共享 DB / Express app / service 的测试改为 `beforeEach/afterEach`，每个 `it` 使用独立临时 SQLite 数据库。
- 补齐原本依赖前序测试写入状态的自包含 fixture，例如：
  - processing flow stats 的 IN_PROGRESS 步骤
  - follow-up NPS 的独立评分数据
  - custom fields、inventory batch、shift template 等列表前置数据
- 修复前端组件测试的 DOM 清理与重复按钮查询。
- `flaky-detect.mjs` 从文件级 shuffle 升级为完整 `--sequence.shuffle`。

## 验证

- 单次完整 shuffle：234 files / 2381 tests 全绿。
- `test:flaky` 两轮完整 shuffle：全绿。
- typecheck、lint、knip、unit tests：通过。
- `test:coverage`：95.09% / 86.54% / 98.98% / 97.09%。
- `test:mutation`：37.80，高于 break 阈值 35。

## 说明

完整 shuffle 现在成为 CI flaky 门禁的一部分。历史顺序依赖不再需要文件级 shuffle 规避。

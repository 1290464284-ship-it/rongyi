# 缺陷台账

日期：2026-08-13

基线：`19176b8` 之后，包含组件 barrel 下线清理。

## 用途

- 这是当前质量冻结期间的唯一缺陷入口。
- 关闭规则：必须有复现步骤、根因分类、修复、回归测试和验收证据。
- 不再使用“第 N 轮审计”来统计问题，改用“OPEN/CLOSED 数量”和“根因类别重复率”。

## 状态约定

- `OPEN`：尚未开始。
- `IN_PROGRESS`：正在修复。
- `VERIFIED`：已修复且门禁通过。
- `WONTFIX`：有明确产品/架构决策，不修。
- `ACCEPTED`：已知并接受，记录原因。

## 当前指标

- 服务端覆盖率：95.04 / 86.63 / 99.04 / 97.05
- Web 覆盖率：95.46 / 90.26 / 98.12 / 97.58
- 测试：229 文件 / 2355 用例
- 重复文件：0 个 MD5 重复
- `knip`：通过
- 组件 barrel：已下线 11 个未使用组件

## 已关闭记录

| 编号 | 问题 | 根因类别 | 关闭方式 |
|---|---|---|---|
| R53-01 | 服务端覆盖率低于 95/85 门禁 | 测试债务 | 补边界/并发/异常测试，恢复 95.04/86.63 |
| R54-01 | 未使用组件继续从 barrel 导出 | 死代码/API 表面积 | 从 `components/index.tsx` 下线 11 个组件 |
| R55-01 | 已下线但未物理删除的 11 个组件 | 死代码/API 表面积 | 删除组件文件、调整 `primitives.spec.tsx`、移除 `MultiSelect.spec.tsx` |
| R55-02 | 幂等 key 未绑定请求体 | 安全/幂等 | 增加 `requestBodyHash` 维度并覆盖全部生产 `withIdempotency` 调用 |
| R55-03 | 父单金额与明细金额不一致 | 一致性 | 通用 treatmentPlanItems CRUD 后自动调用 `reconcilePlanTotal` |
| R55-04 | 审计缓冲生产模式错误路径未直接覆盖 | 可观测性 | 增加 shutdown/flush/retry/console 分支测试 |
| R55-05 | PRAGMA 列缓存依赖手工清缓存 | 可维护性 | 缓存跟随 `schema_version` 自动失效，保留显式清缓存作为兜底 |
| R55-06 | 历史审计文档漂移 | 文档漂移 | 在归档索引顶部增加“以最新代码和门禁为准”声明 |
| R55-07 | 同步冲突解析 rollback 失败分支 | 并发测试 | 增加 ROLLBACK 失败仍保留原始错误的测试 |
| R55-08 | 治疗计划明细乐观更新 changes=0 | 并发测试 | 通过 prepare 故障注入覆盖 setItemDiscount 竞态分支 |
| R55-09 | 退款状态机乐观更新 changes=0 | 并发测试 | 通过 prepare 故障注入覆盖 approve/process 竞态分支 |

## 开放缺陷

| 编号 | 优先级 | 根因类别 | 问题 | 现状与风险 | 建议处理 |
|---|---:|---|---|---|---|

## 已接受（明确决策）

| 编号 | 决策 |
|---|---|
| D-003 | 当前 CSV 导出按 200 行分页写出且设置 100 万行上限，桌面端内存可控；真流式收益低，暂不实现。 |
| D-007 | legacy 导入仍是真实启动路径，保留到产品正式放弃 legacy 导入后再删除。 |

## 根因类别汇总

| 类别 | OPEN 数 | 主要编号 |
|---|---:|---|
| 一致性 | 0 | - |
| 安全/幂等 | 0 | - |
| 性能 | 0 | - |
| 可维护性 | 0 | - |
| 可观测性 | 0 | - |
| 死代码 | 0 | - |
| 架构/兼容 | 0 | - |
| 文档漂移 | 0 | - |
| 并发测试 | 0 | - |

## 验收总则

1. 所有修复必须有对应回归测试。
2. 所有修复必须通过 `typecheck`、`lint`、`knip`、`test:coverage`。
3. 涉及打包/启动/备份/恢复的修复必须通过对应 smoke 或 verify。
4. 关闭条目必须写回本台账，不能只依赖 commit message。
